.env
.env.local
venv/
__pycache__/
*.pyc
.pytest_cache/
node_modules/
.next/
frontend/.next/
frontend/node_modules/
*.egg-info/
dist/
build/
.mypy_cache/
.ruff_cache/
.quality/report.json
orchestrator/state.db
orchestrator/state.db-journal
orchestrator/state.db-wal
orchestrator/state.db-shm
orchestrator/logs/
*.log
.forge/logs/archive/

# Local Claude Code session state (per-session cache, not source)
.claude/

# spec-kit / specify CLI scaffolding — not part of ATLAS source.
# Root-anchored (leading /) so this does NOT accidentally match
# docs/specs/*-criteria.yaml, which ARE source-of-truth and must be
# committed (they drive .quality/checks.py product dim).
/.specify/
/specs/

