version: "1.5"
name: "ATLAS V1.5 — Quality Retrofit"
description: >
  Retrofit V1 to meet the seven-dimension quality standard, then establish the
  autonomous orchestrator as the mechanism for every subsequent chunk. C1-C4
  are bootstrapped outside the orchestrator; C5 onward run through it.

# Global settings the runner consults for every chunk.
settings:
  repo_root: /home/ubuntu/atlas
  quality:
    script: .quality/checks.py
    report: .quality/report.json
    # Legacy composite — retained for C1–FD4 chunks that pre-date S1.
    # Post-S1, the gate no longer reads min_overall; it reads
    # min_per_gating_dim and iterates .quality/report.json's `dims` map.
    min_overall: 80
    min_per_gating_dim: 80
    # Legacy per-dimension floors (pre-S1). Kept so mid-flight chunks
    # don't break if they roll back onto this plan. S1 drops devops+docs
    # and adds backend+product (backend+product start non-gating).
    min_dimensions:
      security: 80
      code: 70
      architecture: 80
      frontend: 70
      devops: 70
      docs: 75
    # Post-S1 gating flags. S1 ships backend+product as non-gating;
    # V1.6 R1 is the chunk that flips them to gating: true.
    gating:
      security: true
      code: true
      architecture: true
      api: true
      frontend: true
      backend: true
      product: true
  retry:
    max_attempts: 3
    backoff_seconds: 30
  claude:
    binary: claude
    model: claude-opus-4-6
    # Chunk sessions are stateless — each spawn is a fresh context.
    extra_args: ["--dangerously-skip-permissions"]
  # Post-chunk sync hook — runs after a chunk transitions to DONE. Keeps
  # git (origin), EC2 filesystem, and the knowledge wiki in lockstep so
  # the next chunk always starts from a clean, deployed, compiled base.
  # Responsibilities: residual commit, push to origin, restart services,
  # spawn /forge-compile headless. See scripts/post-chunk.sh.
  post_chunk:
    enabled: true
    script: scripts/post-chunk.sh

# State machine: PENDING → PLANNING → IMPLEMENTING → TESTING → QUALITY_GATE → DONE
# On failure at any stage: → FAILED → (retry up to max_attempts) → BLOCKED
chunks:
  - id: C1
    title: "Quality engine + frozen standards"
    status: DONE
    bootstrap: true
    artifacts:
      - .quality/standards.md
      - .quality/SOURCE.md
      - .quality/checks.py

  - id: C2
    title: "Orchestrator skeleton — plan, state DB, state machine"
    status: DONE
    bootstrap: true
    depends_on: [C1]
    artifacts:
      - orchestrator/plan.yaml
      - orchestrator/schema.sql
      - orchestrator/state.py
      - orchestrator/plan_loader.py
      - orchestrator/state_machine.py

  - id: C3
    title: "Chunk runner — spawn Claude, run gate, transition state"
    status: DONE
    bootstrap: true
    depends_on: [C2]
    artifacts:
      - orchestrator/runner.py
      - orchestrator/prompts.py

  - id: C4
    title: "CLI + systemd + logging"
    status: DONE
    bootstrap: true
    depends_on: [C3]
    artifacts:
      - orchestrator/cli.py
      - orchestrator/logging_config.py
      - orchestrator/systemd/atlas-forge.service
      - orchestrator/README.md

  # Below here: everything runs THROUGH the orchestrator, one chunk per session.

  - id: C5
    title: "Security hardening — .env.example, CORS allowlist, rate limiting, pip-audit"
    status: PENDING
    depends_on: [C4]
    quality_targets:
      security: 95
    punch_list:
      - Create backend/.env.example with every var the app reads
      - Replace CORS "*" with explicit allowlist from settings
      - Add slowapi rate limiting on all /api/v1/* routes
      - Run pip-audit and resolve or document every advisory
      - Document secret rotation in SECURITY.md

  - id: C6
    title: "Code quality — split god module, shrink long funcs, fix broad excepts"
    status: PENDING
    depends_on: [C4]
    quality_targets:
      code: 85
    punch_list:
      - Split backend/services/jip_data_service.py (505L) by concern
      - Refactor the two >80-line functions flagged by checks.py
      - Replace 5 broad except clauses with specific exception types
      - Rename 36 generic variable names flagged by the scorer
      - Wire ruff + mypy + pytest-cov into pre-commit

  - id: C7
    title: "DevOps — Dockerfile, GitHub Actions, Alembic"
    status: PENDING
    depends_on: [C4]
    quality_targets:
      devops: 90
      architecture: 90
    punch_list:
      - Multi-stage Dockerfile for backend (python:3.12-slim base)
      - docker-compose.yml for local dev (backend + frontend)
      - .github/workflows/ci.yml — ruff, mypy, pytest, quality gate
      - Initialize Alembic, generate baseline migration from current models
      - Document deploy runbook in DEPLOY.md

  - id: C8
    title: "Build dashboard — Next.js UI + nginx + Let's Encrypt"
    status: PENDING
    depends_on: [C4]
    quality_targets:
      frontend: 85
    punch_list:
      - Replace build-dashboard/app.py with Next.js page under frontend/
      - Live chunk status + quality scores + logs tail
      - nginx reverse proxy config
      - certbot / Let's Encrypt for forge.atlas.jsl.internal

  - id: C9
    title: "Frontend hardening — split large components, headless browser checks"
    status: PENDING
    depends_on: [C8]
    quality_targets:
      frontend: 90
    punch_list:
      - Split the 2 components >200 lines flagged by checks.py
      - Add Playwright smoke tests for Market → Sector → Stock flow
      - Lighthouse budget in CI

  - id: C10
    title: "Docs — README, architecture diagram, contributor guide"
    status: PENDING
    depends_on: [C4]
    quality_targets:
      docs: 95
    punch_list:
      - Top-level README.md with quickstart
      - docs/architecture.md with Mermaid diagrams
      - CONTRIBUTING.md covering orchestrator workflow

  - id: C11
    title: "API live checks — deploy + run API dimension of quality gate"
    status: PENDING
    depends_on: [C5, C6, C7]
    quality_targets:
      api: 85
    punch_list:
      - Deploy backend to EC2 via C7 pipeline
      - Run .quality/checks.py --dimension api against live service
      - Confirm overall score ≥ 90 across all seven dimensions

  # =====================================================================
  # S1–S4: Scoring engine rewrite block (pre-V1.6).
  # Fixes the composite/drift problem that let V1 ship without agents.
  # Introduces: 7 dims (adds backend + product, folds/drops devops+docs),
  # passed/eligible formula, no composite, per-dim 80% floor.
  # backend + product dims start non-gating and flip to gating in V1.6 R1.
  # Full specs: docs/specs/chunks/s{1,2,3,4}.md
  # =====================================================================

  - id: S1
    title: "Scoring engine rewrite — 7 dims, no composite, per-dim 80% floor"
    status: PENDING
    depends_on: []
    spec: docs/specs/chunks/s1.md
    quality_targets:
      code: 85
      architecture: 85
    punch_list:
      - Inventory current 53 checks; apply fold/keep/drop table from s1.md
      - Add .quality/dimensions/ package with backend.py and product.py (stub)
      - Gut composite from .quality/checks.py; emit per-dim DimensionResult
      - Fold devops checks (docker_build, alembic_apply, pip_audit) into backend
      - Fold docs checks (readme, claude_md_live, adr_count) into architecture
      - Drop ceremony checks (ssl_expiry, bundle_size, wiki_freshness, etc.)
      - Rewire orchestrator/runner.py _run_quality_gate to iterate gating dims
      - Update plan.yaml settings.quality (already done in this diff)
      - Update forge dashboard UI to render 7 bars, no composite
      - Unit tests for dimension registry + runner gate logic
      - Confirm 5 gating dims all ≥ 80 on current repo before DONE

  - id: S2
    title: "standards.md rewrite — audit 53, add backend+product rubrics"
    status: PENDING
    depends_on: [S1]
    spec: docs/specs/chunks/s2.md
    quality_targets:
      architecture: 90
    punch_list:
      - Audit all 53 existing checks for KEEP/FOLD/DROP (match S1 code)
      - Add missing checks surfaced by Forge Four Laws + project conventions
      - Write full rubric for backend dim (10 checks, plain English + fix + severity)
      - Write product dim framing + worked example (V1 criterion 7)
      - Strip obsolete content (four platforms, composite formula, fix prompts)
      - Add .quality/verify_doc_matches_code.py as new architecture check
      - Every check in checks.py ↔ entry in standards.md (bidirectional)
      - .quality/CHANGES.md with fold/drop/add log

  - id: S3
    title: "v1-criteria.yaml — wire §24.3 into product dim"
    status: PENDING
    depends_on: [S2]
    spec: docs/specs/chunks/s3.md
    quality_targets:
      architecture: 85
    punch_list:
      - Write docs/specs/v1-criteria.schema.json
      - Transcribe all 15 §24.3 criteria to docs/specs/v1-criteria.yaml
      - Implement 5 check types (http_contract, sql_count, sql_invariant, python_callable, file_exists)
      - Flesh out .quality/dimensions/product.py (S1 left it stubbed)
      - Run product dim; confirm ~86% (13/15) with criteria 7+12 failing
      - Add V1 completion progress bar to forge dashboard
      - Product dim stays gating:false (V1.6 R1 flips it)

  - id: S4
    title: "CLAUDE.md surgery — strip to 80 lines, operational rules only"
    status: PENDING
    depends_on: [S3]
    spec: docs/specs/chunks/s4.md
    quality_targets:
      architecture: 90
    punch_list:
      - Move Critical Schema Facts → docs/architecture/critical-schema-facts.md
      - Move Data Flow → docs/architecture/data-flow.md
      - Move Technology Stack → docs/architecture/tech-stack.md
      - Delete V1 criteria list (now in v1-criteria.yaml)
      - Delete Build Order V1→V10 (now in spec)
      - Delete Hard Stop Conditions (now in standards.md)
      - Rewrite remaining CLAUDE.md to 80–120 lines, operational only
      - Preserve Four Laws, System Guarantees, post-chunk invariant, Step 0 protocol verbatim
      - Verify all pointers resolve; grep-verify no scope strings remain
      - All 5 gating dims still ≥ 80 post-rewrite

  # --- L1-MIN — minimal forge runner loop (retroactive registration) ---

  - id: L1-MIN
    title: "Minimal forge runner loop — first working end-to-end spawn"
    status: DONE


  # --- L2-RUNNER — forge-runner on Claude Agent SDK (replaces ralph) ---

  - id: L2-RUNNER
    title: "forge-runner: Claude Agent SDK loop replacing ralph"
    status: PENDING
    spec: specs/003-forge-runner/
    quality_targets:
      security: 100
      code: 80
      architecture: 85
    punch_list:
      - "scripts/forge_runner/ package implemented per specs/003-forge-runner/tasks.md (T001–T062)"
      - "alembic migration adds started_at / runner_pid / failure_reason to chunks table"
      - "Stage protocol + LocalPickStage/LocalImplementStage/LocalVerifyStage/LocalLoopAdvanceStage + HostedStageBase stub"
      - "secrets.scrub + API-key regex check registered in .quality/checks.py security dim"
      - ".ralph/ + .ralphrc removed, CLAUDE.md references updated to forge-runner"
      - "pytest tests/forge_runner/ ≥80% coverage, mypy strict clean, ruff clean"
      - "FORGE_RUNNER_LIVE=1 smoke test passes (real SDK session)"
      - "V1-1 re-ships cleanly under forge-runner as SC-001 acceptance gate (separate dry verification; V1-1 already DONE)"
      - "Per-chunk runner overhead <5s across 9 canned chunks (SC-008 folded into T058)"

  # --- V2 — MF Slice -------------------------------------------------

  - id: V2-1
    title: "MF Pydantic contracts + router skeleton"
    status: PENDING
    quality_targets:
      api: 85
      code: 80
    punch_list:
      - "Every endpoint in contracts/mf-api.md has a Pydantic response model"
      - "Router mounted"
      - "pytest tests/api/test_mf_contracts.py green"
      - "Zero float occurrences in backend/models/mf.py and backend/routes/mf.py"

  - id: V2-2
    title: "JIP client MF extension"
    status: PENDING
    depends_on: [V2-1]
    quality_targets:
      code: 80
    punch_list:
      - "Every method round-trips recorded cassettes"
      - "is_etf=true rows filtered out in mf_universe"
      - "No direct de_* SQL anywhere in the repo"

  - id: V2-3
    title: "MF computations: RS momentum + quadrant + manager alpha"
    status: PENDING
    depends_on: [V2-2]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Fixture of 50 funds produces quadrants matching hand-computed values"
      - "rs_momentum_28d exact Decimal equality on 10 fixture funds"
      - "Returns INSUFFICIENT_DATA when <28 days RS history"

  - id: V2-4
    title: "MF category rollup + universe/categories/flows routes"
    status: PENDING
    depends_on: [V2-3]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "/universe fund_count sums to active-subset count returned by JIP"
      - "/categories p50/p90 manager-alpha matches core helper"
      - "/flows returns last 12 months"
      - "/universe p95 <2s warm"

  - id: V2-5
    title: "Fund deep-dive + holdings + sectors routes"
    status: PENDING
    depends_on: [V2-4]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "manager_alpha == derived_rs_composite - nav_rs_composite exact Decimal on 50 funds"
      - "Holdings weights sum ~100% ±1%"
      - "p95 <500ms warm"

  - id: V2-6
    title: "MF decisions generator"
    status: PENDING
    depends_on: [V2-5]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Fixture transition IMPROVING->LEADING writes exactly one row"
      - "Fixture flow +ve->-ve writes exactly one row"
      - "Re-running is a no-op"
      - "V1 decision lifecycle still handles accept/ignore/override on new rows"

  - id: V2-7
    title: "Overlap + holding-stock + history routes"
    status: PENDING
    depends_on: [V2-5]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "Overlap formula exact on 3-fund fixture"
      - "/holding-stock returns correct fund list for seeded fixture"
      - "NAV history surfaces gap days when a partition is missing"

  - id: V2-8
    title: "Pro shell MF page: universe tree + category table + flows panel"
    status: PENDING
    depends_on: [V2-4]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "FM can walk broad -> category -> fund -> deep-dive in under 10s"
      - "lakh/crore formatting throughout"
      - "V1 pages untouched and still green"

  - id: V2-9
    title: "Pro shell MF deep-dive panel"
    status: PENDING
    depends_on: [V2-5, V2-7, V2-8]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "Every pillar renders from a single /mf/{mstar_id} fetch"
      - "RS history sparkline loads async"
      - "Overlap widget calls /overlap and renders common holdings ranked by min(w_a, w_b)"

  - id: V2-10
    title: "V2 integration suite + completion criteria validation"
    status: PENDING
    depends_on: [V2-6, V2-7, V2-9]
    quality_targets:
      code: 80
      devops: 80
      docs: 80
    punch_list:
      - "scripts/validate-v2.py exits 0"
      - "Every SC-001..SC-009 mapped to an assertion"
      - "V1 integration tests 100% green"
      - ".quality/checks.py product dim >=80% for V2"


  # --- V1 Completion (V1.ext) -------------------------------------------------

  - id: V1-1
    title: "atlas_intelligence + atlas_decisions schema parity"
    status: PENDING
    quality_targets:
      code: 80
    punch_list:
      - "alembic upgrade head succeeds"
      - "pgvector extension present"
      - "HNSW index exists per EXPLAIN"
      - "pytest tests/db/test_intelligence_schema.py green"
      - "Both migrations reversible (alembic downgrade -1 works)"
      - "Zero float columns"
      - "V1 regression green"

  - id: V1-2
    title: "Embedding adapter (OpenAI + nomic fallback)"
    status: PENDING
    depends_on: [V1-1]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Unit tests cover OpenAI success, OpenAI 5xx fallback, missing key fallback, empty string ValueError, batch of 50"
      - "Returned vectors length == 1536 in every path"
      - "V1 regression green"

  - id: V1-3
    title: "Intelligence writer service + intelligence API routes"
    status: PENDING
    depends_on: [V1-2]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "pytest green"
      - "store_finding twice produces one row"
      - "Vector search asserts ordering by similarity on a 5-row fixture"
      - "EXPLAIN shows HNSW index hit"
      - "/api/v1/intelligence p95 <300ms warm against 1k-row fixture"
      - "Zero float in financial fields"
      - "V1 regression green"

  - id: V1-4
    title: "rs-analyzer agent"
    status: PENDING
    depends_on: [V1-3]
    quality_targets:
      code: 80
    punch_list:
      - "Unit test against 30-equity fixture writes the expected number of transition findings + 1 summary"
      - "Re-run idempotent"
      - "Zero de_* SQL — only JIP client"
      - "Zero float in financial fields"
      - "V1 regression green"

  - id: V1-5
    title: "sector-analyst agent"
    status: PENDING
    depends_on: [V1-4]
    quality_targets:
      code: 80
    punch_list:
      - "Unit test on 31-sector fixture with 3 rotations + 2 divergences writes exactly 5 transition findings + 1 summary"
      - "Re-run idempotent"
      - "Reads only via JIP client"
      - "Zero float"
      - "V1 regression green"

  - id: V1-6
    title: "Decisions generator"
    status: PENDING
    depends_on: [V1-5]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "12-finding fixture (8 equity + 4 sector) writes exactly 12 decisions with correct decision_type mapping and supporting_data references"
      - "Re-run is a clean no-op"
      - "Zero float"
      - "Existing accept/ignore/override flow on backend/routes/decisions.py still passes"
      - "V1 regression green"

  - id: V1-7
    title: "Pipeline runner + systemd timer"
    status: PENDING
    depends_on: [V1-6]
    quality_targets:
      code: 80
    punch_list:
      - "python -m atlas.pipeline run --data-as-of=2026-04-13 exits 0 on cassettes and writes the floors"
      - "pytest tests/integration/test_v1_pipeline.py green"
      - "Re-run idempotency proven"
      - "systemd-analyze verify passes"
      - "Pipeline duration <5min on cassette fixture"
      - "data_as_of, rows read, findings written, decisions written, duration_ms visible in journalctl"
      - "V1 regression green"

  - id: V1-8
    title: "Frontend wiring: finding chips + decision card + sector badge + Playwright"
    status: PENDING
    depends_on: [V1-7]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "npm test + npx playwright test v1-fm-flow.spec.ts green against backend with seeded findings/decisions"
      - "Lighthouse budget unchanged"
      - "lakh/crore formatting throughout"
      - "V1 pages untouched and still green"
      - "Backend regression green"

  - id: V1-9
    title: "V1 completion validation + product dim flip to gating"
    status: PENDING
    depends_on: [V1-7, V1-8]
    quality_targets:
      architecture: 85
      code: 80
      devops: 80
      docs: 80
    punch_list:
      - "scripts/validate-v1-completion.py exits 0"
      - "All 15 v1-criteria rows green"
      - "Product dim is gating:true in plan.yaml"
      - "Two consecutive .quality/checks.py runs PASS"
      - "V1 integration tests 100% green"
      - "Memory updated"
      - "Backend + frontend regression green"


  # --- V2-UQL-AGG -------------------------------------------------

  - id: V2-UQL-AGG-1
    title: "Scaffold backend/services/uql/ package"
    status: PENDING
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "Package imports cleanly (python -c 'import backend.services.uql')"
      - "Each skeleton file has a module docstring"
      - "Test suite still green (no behavior change yet)"

  - id: V2-UQL-AGG-2
    title: "Extend Pydantic schemas for UQL v2"
    status: PENDING
    depends_on: [V2-UQL-AGG-1]
    quality_targets:
      code: 80
    punch_list:
      - "pytest tests/unit/test_uql_schemas.py -v passes"
      - "Every validator branch has a covering test"
      - "Schema matches specs/004-uql-aggregations/contracts/uql_query.schema.json"

  - id: V2-UQL-AGG-3
    title: "Implement §20.5 error envelope + exception handler"
    status: PENDING
    depends_on: [V2-UQL-AGG-1, V2-UQL-AGG-2]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/test_uql_errors.py -v passes"
      - "Envelope shape matches specs/004-uql-aggregations/contracts/error_envelope.schema.json for every code"

  - id: V2-UQL-AGG-4
    title: "Implement entity registry"
    status: PENDING
    depends_on: [V2-UQL-AGG-1]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/test_uql_registry.py -v passes"
      - "Every entity resolves"
      - "Every field has a valid SQL expression"
      - "Indexed columns cover the common filter paths for equity + mf"

  - id: V2-UQL-AGG-5
    title: "Implement safety enforcement"
    status: PENDING
    depends_on: [V2-UQL-AGG-2, V2-UQL-AGG-3, V2-UQL-AGG-4]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/test_uql_safety.py -v passes"
      - "Every rejection path covered"
      - "No false positives on legitimate queries"

  - id: V2-UQL-AGG-6
    title: "Implement _meta builder"
    status: PENDING
    depends_on: [V2-UQL-AGG-2, V2-UQL-AGG-4]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/test_uql_meta.py -v passes"
      - "Staleness branches covered"
      - "Pagination math (has_more, next_offset) covered"

  - id: V2-UQL-AGG-7
    title: "Extend JIPDataService with timeout-wrapped execute_sql_plan"
    status: PENDING
    depends_on: [V2-UQL-AGG-3]
    quality_targets:
      code: 80
    punch_list:
      - "pytest tests/unit/test_jip_service_timeout.py -v passes"
      - "A pg_sleep(5) query returns 504 within 2.5s wall-clock"
      - "Normal queries unaffected"

  - id: V2-UQL-AGG-8
    title: "Implement aggregation SQL builders (12 functions)"
    status: PENDING
    depends_on: [V2-UQL-AGG-3, V2-UQL-AGG-4]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/test_uql_aggregations.py -v passes"
      - "Every function emits the expected SQL fragment"
      - "Threshold validation covered"

  - id: V2-UQL-AGG-9
    title: "Implement snapshot + aggregation optimizer"
    status: PENDING
    depends_on: [V2-UQL-AGG-4, V2-UQL-AGG-5, V2-UQL-AGG-8]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/test_uql_optimizer.py -v passes"
      - "Both translation paths produce parameterized SQL (no string interpolation of values)"
      - "Sort alias/column validation covered"

  - id: V2-UQL-AGG-10
    title: "Implement timeseries optimizer"
    status: PENDING
    depends_on: [V2-UQL-AGG-4, V2-UQL-AGG-5]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/test_uql_timeseries.py -v passes"
      - "Granularity and single-entity checks covered"

  - id: V2-UQL-AGG-11
    title: "Implement template registry"
    status: PENDING
    depends_on: [V2-UQL-AGG-2, V2-UQL-AGG-3]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/test_uql_templates.py -v passes"
      - "Adding a new template requires touching only templates.py (verified via directory diff in the test)"

  - id: V2-UQL-AGG-12
    title: "Implement include system resolvers"
    status: PENDING
    depends_on: [V2-UQL-AGG-4, V2-UQL-AGG-7]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/test_uql_includes.py -v passes"
      - "N+1 guard asserts exactly 1 + len(include_modules - {identity}) statements for a 50-row list query"

  - id: V2-UQL-AGG-13
    title: "Implement engine dispatcher + legacy builder"
    status: PENDING
    depends_on: [V2-UQL-AGG-5, V2-UQL-AGG-6, V2-UQL-AGG-7, V2-UQL-AGG-8, V2-UQL-AGG-9, V2-UQL-AGG-10, V2-UQL-AGG-11, V2-UQL-AGG-12]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/test_uql_engine.py -v passes"
      - "Every dispatch path emits exactly one uql.execute log event with the FR-015 fields"

  - id: V2-UQL-AGG-14
    title: "Wire /api/v1/query + /api/v1/query/template"
    status: PENDING
    depends_on: [V2-UQL-AGG-13]
    quality_targets:
      api: 85
      code: 80
    punch_list:
      - "curl smoke tests return 200 for a sample aggregation, timeseries, and template request against the dev backend"

  - id: V2-UQL-AGG-15
    title: "Transpile stocks fixed endpoints through UQL"
    status: PENDING
    depends_on: [V2-UQL-AGG-13]
    quality_targets:
      api: 85
      code: 80
    punch_list:
      - "tests/api/test_stocks.py passes unchanged"
      - "grep shows 'from backend.services.uql import' at top of file"

  - id: V2-UQL-AGG-16
    title: "Transpile mf fixed endpoints through UQL"
    status: PENDING
    depends_on: [V2-UQL-AGG-13]
    quality_targets:
      api: 85
      code: 80
    punch_list:
      - "tests/api/test_mf.py passes unchanged"
      - "Module imports backend.services.uql at top"

  - id: V2-UQL-AGG-17
    title: "Wire single-entity GET with include system"
    status: PENDING
    depends_on: [V2-UQL-AGG-15, V2-UQL-AGG-12]
    quality_targets:
      api: 85
      code: 80
    punch_list:
      - "curl 'http://localhost:8000/api/v1/stocks/HDFCBANK?include=rs,conviction' returns HTTP 200 with _meta.includes_loaded set"

  - id: V2-UQL-AGG-18
    title: "Integration tests: aggregations"
    status: PENDING
    depends_on: [V2-UQL-AGG-14]
    quality_targets:
      code: 80
    punch_list:
      - "pytest tests/api/test_query_aggregations.py -v passes against dev DB"

  - id: V2-UQL-AGG-19
    title: "Integration tests: timeseries"
    status: PENDING
    depends_on: [V2-UQL-AGG-14]
    quality_targets:
      code: 80
    punch_list:
      - "pytest tests/api/test_query_timeseries.py -v passes against dev DB"

  - id: V2-UQL-AGG-20
    title: "Integration tests: templates"
    status: PENDING
    depends_on: [V2-UQL-AGG-14]
    quality_targets:
      code: 80
    punch_list:
      - "pytest tests/api/test_query_templates.py -v passes against dev DB"

  - id: V2-UQL-AGG-21
    title: "Integration tests: multi-entity dispatch"
    status: PENDING
    depends_on: [V2-UQL-AGG-14]
    quality_targets:
      code: 80
    punch_list:
      - "pytest tests/api/test_query_multi_entity.py -v passes against dev DB with all four entities loaded"

  - id: V2-UQL-AGG-22
    title: "Integration tests: safety limits"
    status: PENDING
    depends_on: [V2-UQL-AGG-14]
    quality_targets:
      code: 80
    punch_list:
      - "pytest tests/api/test_query_safety.py -v passes"

  - id: V2-UQL-AGG-23
    title: "Integration tests: §20.5 error envelope"
    status: PENDING
    depends_on: [V2-UQL-AGG-14]
    quality_targets:
      code: 80
    punch_list:
      - "pytest tests/api/test_query_errors.py -v passes"
      - "jsonschema validation passes on every response"

  - id: V2-UQL-AGG-24
    title: "Integration tests: include system"
    status: PENDING
    depends_on: [V2-UQL-AGG-17]
    quality_targets:
      code: 80
    punch_list:
      - "pytest tests/api/test_query_include.py -v passes"

  - id: V2-UQL-AGG-25
    title: "Integration tests: fixed-endpoint transpile invariant + meta determinism"
    status: PENDING
    depends_on: [V2-UQL-AGG-15, V2-UQL-AGG-16]
    quality_targets:
      code: 80
    punch_list:
      - "Both test files pass against dev DB"
      - "tests/api/test_stocks.py and tests/api/test_mf.py still pass unchanged"

  - id: V2-UQL-AGG-26
    title: "OpenAPI docs + structured logging guards"
    status: PENDING
    depends_on: [V2-UQL-AGG-14]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "/api/v1/docs renders the new routes with examples"
      - "tests/unit/test_uql_logging.py passes"

  - id: V2-UQL-AGG-27
    title: "Run scripts/check-api-standard.py (gate flip)"
    status: PENDING
    depends_on: [V2-UQL-AGG-18, V2-UQL-AGG-19, V2-UQL-AGG-20, V2-UQL-AGG-21, V2-UQL-AGG-22, V2-UQL-AGG-23, V2-UQL-AGG-24, V2-UQL-AGG-25, V2-UQL-AGG-26]
    punch_list:
      - "Script exits 0 and reports PASS for all six criteria"
      - "Output file committed as evidence"

  - id: V2-UQL-AGG-28
    title: "Run 7-dim quality gate"
    status: PENDING
    depends_on: [V2-UQL-AGG-27]
    punch_list:
      - "All 7 dims ≥80%"
      - "Product dim must factor the now-green api-standard-criteria.yaml alongside v1-criteria.yaml"

  - id: V2-UQL-AGG-29
    title: "Post-deploy smoke probe on EC2"
    status: PENDING
    depends_on: [V2-UQL-AGG-28]
    punch_list:
      - "Both probes return HTTP 200 with populated records and _meta"
      - "atlas-backend.service is active"

  - id: V2-UQL-AGG-30
    title: "Memory + wiki sync"
    status: PENDING
    depends_on: [V2-UQL-AGG-29]
    punch_list:
      - "Memory files reflect V2-UQL-AGG DONE"
      - "/forge-compile exits 0"
      - "Wiki index shows at least one new article for the UQL engine pattern"


  # --- V3 Simulation Slice (retroactive registration) ---------------
  # V3-1..V3-9 shipped from specs/005-v3-simulation-engine/ directly
  # without being registered here. These entries exist so the forge
  # dashboard + roadmap lint agree with orchestrator/state.db. Status
  # is tracked in state.db, not this field.

  - id: V3-1
    title: "Simulation foundations — atlas_simulations table, ORM, contracts, route stubs"
    status: DONE

  - id: V3-2
    title: "Indian FIFO tax engine (pre/post 23-Jul-2024, 4% cess, annual exemption)"
    status: DONE
    depends_on: [V3-1]

  - id: V3-3
    title: "Signal adapters — 7 sources (BREADTH/McCLELLAN/RS/PE/REGIME/SECTOR_RS/SUMMATION)"
    status: DONE
    depends_on: [V3-1]

  - id: V3-4
    title: "Backtest engine + analytics + SimulationService.run"
    status: DONE
    depends_on: [V3-2, V3-3]

  - id: V3-5
    title: "Simulation listing + detail + save + soft-delete + auto-loop re-run"
    status: DONE
    depends_on: [V3-4]

  - id: V3-6
    title: "Portfolio optimizer integration (Riskfolio)"
    status: DONE
    depends_on: [V3-4]

  - id: V3-7
    title: "In-process auto-loop scheduler + drift alert + re-optimization handoff"
    status: DONE
    depends_on: [V3-5, V3-6]

  - id: V3-8
    title: "Pro shell /pro/simulate Simulation Lab page"
    status: DONE
    depends_on: [V3-5]

  - id: V3-9
    title: "V3 polish — product dim wires v2+v3 criteria, validate-v3 script"
    status: DONE
    depends_on: [V3-7, V3-8]


  # --- V4 -------------------------------------------------

  - id: V4-1
    title: "Portfolio foundations — ORM, Alembic migration, Pydantic contracts, route stubs"
    status: PENDING
    quality_targets:
      api: 85
      code: 80
      docs: 80
    punch_list:
      - "alembic upgrade head green against dev DB"
      - "psql describe on atlas_portfolios and the four sibling tables shows UUID pk, tz-aware timestamps, Numeric(20,4) money columns, FK indexes"
      - "python -c 'import backend.routes.portfolio, backend.models.portfolio, backend.contracts.portfolio' clean"
      - "pytest tests/unit/portfolio/test_models.py tests/api/test_portfolio_stubs.py -v green"
      - "scripts/check-api-standard.py green against the new route module (every route UQL-backed, §17/§18/§20 compliant)"
      - "scripts/check-spec-coverage.py green (v4-criteria.yaml keys map to quality dims)"
      - "No de_* table touched"
      - "verified by grepping the migration"
      - "No print() in backend/routes/portfolio.py or backend/services/portfolio/"

  - id: V4-2
    title: "CAMS import + scheme mapping — casparser wrapper, fuzzy match, persistence"
    status: PENDING
    depends_on: [V4-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "pip list | grep casparser shows the pinned version in the EC2 venv"
      - "POST /api/v1/portfolio/import-cams accepts the redacted CAS fixture and returns a PortfolioImportResult with N holdings, per-holding confidence, and a needs-review bucket"
      - "Second import with a pinned override in atlas_scheme_mapping_overrides short-circuits fuzzy match (covered by regression test)"
      - "pytest tests/unit/portfolio/test_cams_import.py tests/unit/portfolio/test_scheme_mapper.py tests/api/test_portfolio_import.py -v green"
      - "No write to any de_* table (grep the diff)"
      - "All money values Decimal through the whole path"

  - id: V4-3
    title: "Portfolio analysis engine — per-holding + portfolio-level, read-only JIP"
    status: PENDING
    depends_on: [V4-2]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/portfolio/test_analysis.py tests/api/test_portfolio_analysis.py -v green"
      - "Determinism regression passes (same data_as_of → byte-equal output)"
      - "Graceful-degradation regression passes (one JIP endpoint starved → analysis still returns with unavailable list)"
      - "scripts/check-api-standard.py green on the updated routes"
      - "No direct SQL in analysis.py (everything through jip_mf_service + UQL)"
      - "Every response field traceable to source table + formula"

  - id: V4-4
    title: "Brinson attribution — allocation + selection + interaction"
    status: PENDING
    depends_on: [V4-3]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/portfolio/test_attribution.py tests/api/test_portfolio_attribution.py -v green"
      - "Hand-calculated fixture reconciliation passes within documented tolerance"
      - "Determinism regression passes"
      - "scripts/check-api-standard.py green"
      - "Response includes formula + tolerance + data_as_of"

  - id: V4-5
    title: "Riskfolio-Lib optimization — mean-variance + HRP under SEBI constraints"
    status: PENDING
    depends_on: [V4-3]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "pip list | grep riskfolio shows the pinned version in the EC2 venv"
      - "pytest tests/unit/portfolio/test_optimization.py tests/api/test_portfolio_optimize.py -v green"
      - "Mean-variance and HRP both return distinct weight vectors on identical inputs"
      - "Determinism regression passes (same inputs + data_as_of → byte-equal weights)"
      - "Infeasibility test returns structured error with violated constraint id"
      - "Measured per-model latency <10s on production-size candidate universe"
      - "scripts/check-api-standard.py green"

  - id: V4-6
    title: "Daily monitoring + tax harvesting (delegates to V3 FIFO)"
    status: PENDING
    depends_on: [V4-3]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/portfolio/test_monitoring.py tests/unit/portfolio/test_tax_harvest.py -v green"
      - "grep confirms tax_harvest.py contains no rate tables, no cess constants, no FIFO logic — only imports from backend.services.simulation.tax.fifo"
      - "Bit-for-bit match with V3 FIFO golden fixture"
      - "Non-trading-day regression passes (zero alerts)"
      - "Consecutive-day LAGGING trigger fires at exactly 28 trading days, not 27 or 29"
      - "Deterministic alerts for a given data_as_of"

  - id: V4-7
    title: "Portfolio dashboard — /pro/portfolio page in fie2 frontend"
    status: PENDING
    depends_on: [V4-2, V4-3, V4-4, V4-5, V4-6]
    quality_targets:
      frontend: 80
    punch_list:
      - "npm test green"
      - "TypeScript strict-mode clean"
      - "Manual browser verification on EC2 /pro/portfolio/{id} renders the real imported V4-2 fixture — holdings table, weighted RS card, sector concentration chart, attribution panel, optimizer panel all populated with live API data"
      - "Every ₹ value renders lakh/crore"
      - "Drill-down from aggregate tiles returns constituent holdings"
      - "CSV export works on HoldingsTable"
      - "No synthetic data in any component (Four Laws L2)"
      - "Backend-first rule honored — this chunk only opens after chunks 2-6 are DONE"



  # --- V5 Intelligence Slice -------------------------------------------------

  - id: V5-1
    title: "Pin V5 deps + enable pgvector extension"
    status: PENDING
    quality_targets:
      code: 80
    punch_list:
      - "pip install -r backend/requirements.txt succeeds"
      - "alembic upgrade head creates the vector extension"
      - "SELECT extname FROM pg_extension WHERE extname='vector' returns 1 row"

  - id: V5-2
    title: "V5 Alembic migration + SQLAlchemy models"
    status: PENDING
    depends_on: [V5-1]
    quality_targets:
      code: 80
    punch_list:
      - "alembic upgrade head clean"
      - "psql describe on atlas_intelligence shows the HNSW index"
      - "SELECT COUNT(*) FROM atlas_intelligence returns 0"
      - "CHECK constraint on atlas_agent_weights rejects weight=0.2 and weight=3.0"
      - "mypy clean on the new models"

  - id: V5-3
    title: "Embeddings service (primary + fallback)"
    status: PENDING
    depends_on: [V5-2]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Unit tests cover primary success, primary-5xx fallback, both-down raises, dimensionality invariance across providers"
      - "pytest tests/unit/intelligence/test_embeddings_fallback.py -v green"

  - id: V5-4
    title: "Intelligence memory service (store_finding + get_relevant_intelligence)"
    status: PENDING
    depends_on: [V5-3]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "≥95% unit coverage on store_finding and get_relevant_intelligence"
      - "tests cover store happy path, every read filter combination, expired-exclusion, Decimal enforcement (float rejected), IST enforcement (naive datetime rejected), and a structlog-context read-path log event per FR-023"
      - "pytest green"

  - id: V5-5
    title: "Intelligence Memory API — 3 read-only routes"
    status: PENDING
    depends_on: [V5-4]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "scripts/check-api-standard.py green for the 3 new routes"
      - "every route returns the standard envelope"
      - "OpenAPI diff vs contracts/v5-api.openapi.yaml shows zero drift"
      - "pytest tests/api/test_intelligence_*.py -v green"

  - id: V5-6
    title: "LLM cost ledger ($2/day rolling window)"
    status: PENDING
    depends_on: [V5-5]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Unit tests cover under-budget, at-budget, over-budget halt, rolling-window arithmetic, and the atlas_alerts side-effect"
      - "pytest green"

  - id: V5-7
    title: "LangGraph agent orchestration scaffold"
    status: PENDING
    depends_on: [V5-6]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Integration test proves a 2-node graph with one source missing still produces a result flagged is_stale=true"
      - "no exception escapes the graph on partial data"

  - id: V5-8
    title: "Briefing writer pipeline (TradingAgents debate fork)"
    status: PENDING
    depends_on: [V5-7]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Integration tests assert: (a) exactly one atlas_briefings row per trading day after one run, (b) rerun updates in place not duplicates, (c) same data_as_of → identical key_signals/theses/conviction/model_used fields, (d) missing upstream source still produces a briefing with staleness flag"

  - id: V5-9
    title: "Global Intelligence API — 5 read-only routes"
    status: PENDING
    depends_on: [V5-8]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "scripts/check-api-standard.py green for all 5 new routes"
      - "pytest tests/api/test_global_*.py -v green"
      - "OpenAPI drift = 0 vs contracts/v5-api.openapi.yaml"

  - id: V5-10
    title: "Goldilocks analyst agent"
    status: PENDING
    depends_on: [V5-5]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Integration test on a real trading day with published Goldilocks ideas produces ≥5 real findings each with evidence pointing to both the Goldilocks source row and the matching RS row"
      - "missing-ticker path logs a data gap without aborting the pipeline"

  - id: V5-10a
    title: "Investor persona agents (4 personas, ai-hedge-fund fork)"
    status: PENDING
    depends_on: [V5-10]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Integration test runs all 4 personas on a real trading day, asserts each persona writes ≥1 finding tagged with its persona identifier and full provenance, and confirms LLM calls pass through the cost ledger"

  - id: V5-11
    title: "Darwinian nightly accuracy scoring + specialist auto-spawn"
    status: PENDING
    depends_on: [V5-9]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Unit tests cover the full window-elapsed matrix (not-yet, just-elapsed, long-elapsed), the non-scored-agent skip, the 60-prediction rolling window, the holiday no-op, and the 3-errors-in-5-days specialist-spawn trigger"
      - "pytest green"

  - id: V5-12
    title: "Darwinian daily weight adjustment"
    status: PENDING
    depends_on: [V5-11]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Unit tests cover each quartile, floor and ceiling clamps, even distribution tie-break, and holiday no-op"
      - "DB CHECK constraint rejects any drift"
      - "pytest green"

  - id: V5-13
    title: "Darwinian mutation runner (shadow-mode merge/revert)"
    status: PENDING
    depends_on: [V5-12]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Integration test drives one full merge path and one full revert path on real predictions in shadow mode"
      - "advisory lock prevents a second concurrent cycle on the same agent"
      - "mutation history persists the outcome"
      - "pytest green"

  - id: V5-14
    title: "Pro shell /pro/global page"
    status: PENDING
    depends_on: [V5-9]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "Smoke test opens /pro/global against real API, asserts real numbers render in lakh/crore, dates render DD-MMM-YYYY IST, each panel has a visible data_as_of, and a forced single-panel failure degrades independently without crashing the page"

  - id: V5-15
    title: "Pro shell /pro/intelligence page"
    status: PENDING
    depends_on: [V5-5]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "Smoke test runs a real semantic query, asserts the top-k findings render with confidence + evidence + agent + timestamp, and asserts the empty-state message appears when min_confidence is set to 0.99 with no matches"

  - id: V5-16
    title: "V5 polish + 7-dimension quality gate + post-chunk sync"
    status: PENDING
    depends_on: [V5-10, V5-10a, V5-13, V5-14, V5-15]
    quality_targets:
      devops: 80
      docs: 80
    punch_list:
      - "All 7 quality dimensions ≥ 80%"
      - "scripts/check-spec-coverage.py green"
      - "scripts/check-api-standard.py green"
      - "SC-001..SC-012 each have recorded evidence"
      - "scripts/post-chunk.sh exits 0"
      - "MEMORY.md project_v15_chunk_status.md shows V5-1..V5-16 (plus V5-10a) all DONE"


  # --- V6 TradingView Slice -------------------------------------------------

  - id: V6-1
    title: "V6 DB migration: atlas_tv_cache + atlas_watchlists + atlas_alerts"
    status: PENDING
    quality_targets:
      code: 80
    punch_list:
      - "alembic upgrade head clean"
      - "describe all 3 tables shows correct schema"
      - "mypy clean on new models"
      - "SELECT COUNT(*) FROM atlas_tv_cache returns 0"

  - id: V6-2
    title: "TV MCP bridge service wrapper"
    status: PENDING
    depends_on: [V6-1]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/tv/test_bridge_timeout.py -v green"
      - "timeout raises TVBridgeUnavailableError (not 500)"
      - "mypy clean"

  - id: V6-3
    title: "TV cache service + webhook route"
    status: PENDING
    depends_on: [V6-2]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "POST /api/webhooks/tradingview returns 200 on valid payload"
      - "403 on bad/missing secret"
      - "422 on malformed body"
      - "cache hit returns without bridge call"
      - "stale entry served with is_stale=true while background refresh queued"

  - id: V6-4
    title: "TV TA + screener + fundamentals API routes"
    status: PENDING
    depends_on: [V6-3]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "GET /api/tv/ta/HDFCBANK returns recommendation_1d, oscillator_score, ma_score"
      - "meta.is_stale present"
      - "scripts/check-api-standard.py green for tv routes"
      - "pytest tests/api/test_tv_ta.py -v green"

  - id: V6-5
    title: "Stock conviction pillar 3 (TV TA) + chart-data endpoint"
    status: PENDING
    depends_on: [V6-4]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "GET /api/stocks/HDFCBANK includes conviction.pillar_3.tv_ta"
      - "when TV down, tv_ta is null and meta.partial_data is true"
      - "GET /api/stocks/HDFCBANK/chart-data?from=2025-01-01&to=2026-01-01 returns ~252 rows with dates+OHLCV+technicals"

  - id: V6-6
    title: "Watchlist CRUD + TV sync API"
    status: PENDING
    depends_on: [V6-3]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "CRUD happy paths green"
      - "POST sync-tv sets tv_synced=true on bridge success"
      - "POST sync-tv returns 503 with error envelope when bridge unavailable"
      - "DELETE sets is_deleted=true"
      - "pytest tests/api/test_watchlists.py -v green"

  - id: V6-7
    title: "Alerts API + alert rules CRUD"
    status: PENDING
    depends_on: [V6-3]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "GET /api/alerts?unread=true returns only unread rows"
      - "source filter works"
      - "mark-read sets is_read=true"
      - "POST /api/alerts/999999/read returns 404"
      - "pytest tests/api/test_alerts.py -v green"

  - id: V6-8
    title: "Pro shell /pro/alerts + /pro/watchlists pages"
    status: PENDING
    depends_on: [V6-5, V6-6, V6-7]
    quality_targets:
      frontend: 80
    punch_list:
      - "npm run build succeeds"
      - "/pro/alerts renders alert list with source badges and timestamps in IST"
      - "mark-read action calls POST /api/alerts/{id}/read"
      - "/pro/watchlists renders list with TV-synced badge"
      - "sync-to-TV button calls POST sync-tv"

  - id: V6-9
    title: "V6 polish + 7-dim quality gate + post-chunk sync"
    status: PENDING
    depends_on: [V6-8]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python .quality/checks.py exits 0 (all 7 dims >=80)"
      - "scripts/check-api-standard.py green"
      - "pytest tests/ -v all green"
      - "scripts/post-chunk.sh V6-9 completes without error"


  # --- V6.5 Derived Signal Engine (closes 15% data gap before V7 frontend) ------

  - id: C-DER-1
    title: "Stock Signal Engine — Gold RS + Piotroski"
    status: PENDING
    depends_on: [V6T-5]
    spec: docs/specs/chunks/c-der-1.md
    quality_targets:
      code: 82
      security: 90
      architecture: 85
      api: 90
    punch_list:
      - "GET /api/v1/stocks/{symbol} returns gold_rs with signal AMPLIFIES_BULL/NEUTRAL/FRAGILE/AMPLIFIES_BEAR"
      - "GET /api/v1/stocks/{symbol} returns piotroski.score (0-9) and grade"
      - "Gold RS ratio uses GLD from de_global_price_daily over 63 trading days"
      - "Piotroski F1-F9 checks all implemented and tested"
      - "Missing GLD data returns gold_rs=null without 500"
      - "Missing fundamentals history returns piotroski=null without 500"
      - "scripts/check-api-standard.py green"
      - "pytest tests/services/test_derived_signals.py -v >=18 tests green"
      - "mypy clean"
      - "scripts/post-chunk.sh C-DER-1 completes"

  - id: C-DER-2
    title: "4-Factor Conviction Engine + Action + Urgency + Screener"
    status: PENDING
    depends_on: [C-DER-1]
    spec: docs/specs/chunks/c-der-2.md
    quality_targets:
      code: 82
      security: 90
      architecture: 85
      api: 90
    punch_list:
      - "GET /api/v1/stocks/{symbol} returns four_factor with conviction_level, action_signal, urgency"
      - "4-factor score: Returns RS × Momentum RS (percentile) × Sector RS × Volume RS"
      - "Percentile rank for roc_21 computed in SQL (not Python)"
      - "GET /api/v1/screener returns rows with conviction_level, action_signal, urgency"
      - "Screener universe/sector/conviction/action filters all functional"
      - "Screener pagination (limit/offset) works"
      - "Action derivation: HIGH+ + BULL = BUY; HIGH+ + CORRECTION = ACCUMULATE; MEDIUM = WATCH; LOW falling = REDUCE; AVOID = EXIT"
      - "scripts/check-api-standard.py green"
      - "pytest tests/services/test_conviction_engine.py -v >=20 tests green"
      - "mypy clean"
      - "scripts/post-chunk.sh C-DER-2 completes"

  - id: C-DER-3
    title: "Sector RRG + Sentiment Composite + Regime Enrichment"
    status: PENDING
    depends_on: [C-DER-2]
    spec: docs/specs/chunks/c-der-3.md
    quality_targets:
      code: 82
      security: 90
      architecture: 85
      api: 90
    punch_list:
      - "GET /api/v1/stocks/breadth returns regime.days_in_regime and regime.regime_history (last 5 transitions)"
      - "GET /api/v1/sectors/rrg returns all sectors with rs_score, rs_momentum, quadrant, tail (4 points)"
      - "RRG rs_score normalized to 100-center (mean/stddev)"
      - "RRG quadrant: LEADING/WEAKENING/IMPROVING/LAGGING per formula"
      - "GET /api/v1/sentiment/composite returns composite_score (0-100) and zone"
      - "Sentiment redistributes PCR+flow weights to breadth+fundamentals when de_fo_summary empty"
      - "Sentiment never 500s on empty de_fo_summary or de_flow_daily — degrades gracefully"
      - "Breadth-only sentiment still returns a valid score when 2 components unavailable"
      - "scripts/check-api-standard.py green"
      - "pytest tests/ -v >=16 new tests green"
      - "mypy clean"
      - "scripts/post-chunk.sh C-DER-3 completes"


  # --- V8 Advisor Shell -------------------------------------------------

  - id: V8-1
    title: "V8 DB migration: atlas_advisor_clients + atlas_model_portfolios + atlas_reports"
    status: PENDING
    quality_targets:
      code: 80
    punch_list:
      - "alembic upgrade head clean"
      - "describe all 3 tables correct"
      - "FK index=True on client_id"
      - "mypy clean"
      - "SELECT COUNT(*) FROM atlas_advisor_clients returns 0"

  - id: V8-2
    title: "Client management API with PII encryption"
    status: PENDING
    depends_on: [V8-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "POST creates client"
      - "plaintext name never in DB (encrypted)"
      - "GET /api/advisor/clients/{id} returns decrypted name"
      - "DELETE sets is_deleted=true"
      - "404 on missing id"
      - "pytest tests/api/test_advisor_clients.py -v green"

  - id: V8-3
    title: "Model portfolios CRUD API"
    status: PENDING
    depends_on: [V8-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "CRUD happy paths green"
      - "weight sum validation (optional but recommended)"
      - "soft-delete works"
      - "pytest green"

  - id: V8-4
    title: "Report generator service + API"
    status: PENDING
    depends_on: [V8-2, V8-3]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "POST /api/advisor/reports returns {id, generated_at}"
      - "GET /download returns Content-Type: application/pdf"
      - "404 on missing report id"
      - "pytest green"
      - "no PII in logs"

  - id: V8-5
    title: "Advisor shell: Dashboard + Client List + Client Detail pages"
    status: PENDING
    depends_on: [V8-2]
    quality_targets:
      frontend: 80
    punch_list:
      - "npm run build succeeds"
      - "client list renders with real data"
      - "AUM formatted as lakh/crore"
      - "client detail shows portfolio holdings"

  - id: V8-6
    title: "Advisor shell: Fund Discovery + Model Portfolios pages"
    status: PENDING
    depends_on: [V8-3, V8-5]
    quality_targets:
      frontend: 80
    punch_list:
      - "npm run build succeeds"
      - "AUM filter works with lakh/crore input"
      - "overlap check renders common holdings"
      - "model portfolio create/edit modal saves via API"

  - id: V8-7
    title: "Advisor shell: Reports + Market View pages"
    status: PENDING
    depends_on: [V8-4, V8-6]
    quality_targets:
      frontend: 80
    punch_list:
      - "npm run build succeeds"
      - "generate report button calls POST /api/advisor/reports"
      - "download triggers file download"
      - "market view shows briefing headline and sector summary"

  - id: V8-8
    title: "V8 polish + 7-dim quality gate + post-chunk sync"
    status: PENDING
    depends_on: [V8-7]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python .quality/checks.py exits 0"
      - "scripts/check-api-standard.py green"
      - "pytest tests/ -v all green"
      - "post-chunk sync completes"


  # --- V9 Retail Shell -------------------------------------------------

  - id: V9-1
    title: "V9 DB migration: atlas_retail_goals"
    status: PENDING
    quality_targets:
      code: 80
    punch_list:
      - "alembic upgrade head clean"
      - "FK index=True on portfolio_id"
      - "Numeric(20,4) on money columns"
      - "mypy clean"
      - "SELECT COUNT(*) FROM atlas_retail_goals returns 0"

  - id: V9-2
    title: "Retail fund discovery API (simplified, no raw RS)"
    status: PENDING
    depends_on: [V9-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "GET /api/retail/discover returns funds with traffic_light field (green/amber/red)"
      - "no rs_1m field in response"
      - "GET /api/retail/discover/{mstar_id} includes health_checks_passed and health_checks_total"
      - "pytest green"

  - id: V9-3
    title: "Goal planner service + goals API"
    status: PENDING
    depends_on: [V9-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "test_goal_planner.py: given target=20000000 (2Cr), current=500000, rate=12%, tenure=14y, required SIP matches expected Decimal value"
      - "no float used"
      - "POST /api/retail/goals/calculate returns required_monthly_sip as Decimal"
      - "pytest green"

  - id: V9-4
    title: "Retail shell: Home + Fund Discovery pages (mobile-first)"
    status: PENDING
    depends_on: [V9-2]
    quality_targets:
      frontend: 80
    punch_list:
      - "npm run build succeeds"
      - "discover page shows traffic light badges"
      - "fund detail shows health checks count"
      - "no technical jargon"
      - "mobile layout renders correctly at 375px"

  - id: V9-5
    title: "Retail shell: Portfolio + Goals pages"
    status: PENDING
    depends_on: [V9-3, V9-4]
    quality_targets:
      frontend: 80
    punch_list:
      - "npm run build succeeds"
      - "portfolio page shows value in lakh/crore"
      - "goal form calculates required SIP on input change"
      - "progress bar shows current_value/target_amount%"
      - "save stores via POST /api/retail/goals"

  - id: V9-6
    title: "Retail shell: SIP Calculator + Alerts + Learn pages"
    status: PENDING
    depends_on: [V9-5]
    quality_targets:
      frontend: 80
    punch_list:
      - "npm run build succeeds"
      - "SIP calculator shows projected corpus chart"
      - "no technical jargon on any page"
      - "learn page renders static content correctly"

  - id: V9-7
    title: "V9 polish + 7-dim quality gate + post-chunk sync"
    status: PENDING
    depends_on: [V9-6]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python .quality/checks.py exits 0 (all 7 dims >=80)"
      - "pytest tests/ -v all green"
      - "scripts/post-chunk.sh V9-7 completes"


  # --- V10 Qlib + Advanced -------------------------------------------------

  - id: V10-1
    title: "V10 DB migration: atlas_qlib_features + atlas_qlib_signals + atlas_events"
    status: PENDING
    quality_targets:
      code: 80
    punch_list:
      - "alembic upgrade head clean"
      - "all 3 tables created"
      - "FK index=True on instrument_id in both qlib tables"
      - "mypy clean"
      - "SELECT COUNT(*) FROM atlas_qlib_features returns 0"

  - id: V10-2
    title: "Alpha158 feature computation (158 factors, pandas vectorized)"
    status: PENDING
    depends_on: [V10-1]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "pytest tests/unit/qlib/test_alpha158.py -v green"
      - "all 158 column names present in output"
      - "KMID formula verified on known values"
      - "no iterrows"
      - "mypy clean"

  - id: V10-3
    title: "Nightly Qlib pipeline: OHLCV → alpha158 → atlas_qlib_features"
    status: PENDING
    depends_on: [V10-2]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Pipeline runs on real OHLCV data"
      - "atlas_qlib_features gains rows for all stocks with >=60 days data"
      - "second run on same day leaves row count unchanged"
      - "structlog shows before/after row counts"
      - "pytest green"

  - id: V10-4
    title: "Event system: producer + HTTP polling + WebSocket endpoint"
    status: PENDING
    depends_on: [V10-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "emit_event writes to atlas_events"
      - "GET /api/events returns events with undelivered filter"
      - "WebSocket client receives event within 2s of emit"
      - "pytest tests/api/test_events_http.py + test_events_ws.py green"

  - id: V10-5
    title: "Wire event emitters into existing subsystems + RS change detector"
    status: PENDING
    depends_on: [V10-4]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Simulation service emits SIMULATION_COMPLETE event on run completion"
      - "RS change detector correctly identifies quadrant changes on known test data"
      - "pytest tests/unit/events/test_rs_change_detector.py green"

  - id: V10-6
    title: "Simulation parameter optimization (Optuna)"
    status: PENDING
    depends_on: [V10-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "POST /api/simulate/optimize with valid body runs >=10 trials and returns best_params"
      - "invalid signal type returns 422 with error envelope"
      - "pytest tests/api/test_simulate_optimize.py green"

  - id: V10-7
    title: "Factor validation (alphalens) + tearsheet API"
    status: PENDING
    depends_on: [V10-3]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "GET /api/qlib/factor-tearsheet/KMID returns {ic_mean, ic_std, quantile_returns, turnover}"
      - "requires atlas_qlib_features to have >=30 days data"
      - "pytest green"

  - id: V10-8
    title: "LightGBM signal model (Phase 2 — best-effort)"
    status: PENDING
    depends_on: [V10-3]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "Model trains on real data when >=90 days features available"
      - "predictions written to atlas_qlib_signals"
      - "model skips gracefully if <90 days available"
      - "pytest green"

  - id: V10-9
    title: "V10 polish + 7-dim quality gate + post-chunk sync"
    status: PENDING
    depends_on: [V10-5, V10-6, V10-7, V10-8]
    quality_targets:
      code: 80
    punch_list:
      - "python .quality/checks.py exits 0 (all 7 dims >=80)"
      - "scripts/check-api-standard.py green"
      - "pytest tests/ -v all green"
      - "post-chunk sync completes"


  # --- V6T -------------------------------------------------

  - id: V6T-1
    title: "Setup: pin tradingview-screener, clean dead config"
    status: DONE
    quality_targets:
      code: 80
    punch_list:
      - "tradingview-screener==3.1.0 in requirements.txt"
      - "tv_bridge_url removed from config"
      - "tv_webhook_secret and tv_cache_ttl_seconds retained"

  - id: V6T-2
    title: "Bridge rewrite + watchlist sync-tv route removal"
    status: PENDING
    depends_on: [V6T-1]
    spec: docs/specs/chunks/v6t-2.md
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "GET /api/tv/ta/RELIANCE returns non-503 with real TV data (Recommend.All field present)"
      - "8+ bridge tests pass in tests/unit/tv/"
      - "POST /api/watchlists/{id}/sync-tv returns 404"
      - "No httpx in bridge.py — uses tradingview_screener.Query directly"
      - "asyncio.to_thread() wraps synchronous get_scanner_data() call"
      - "mypy clean on backend/services/tv/bridge.py"
      - "pytest tests/unit/tv/ tests/routes/test_tv_routes.py all green"

  - id: V6T-3
    title: "TVConvictionPanel component + DeepDivePanel wiring"
    status: PENDING
    depends_on: [V6T-2]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "DeepDivePanel shows TVConvictionPanel with 3 coloured rows for any stock with cached TV data"
      - "Graceful TV data unavailable fallback when tv_ta is null"

  - id: V6T-4
    title: "Stock table TV column + watchlist Refresh TV signals button"
    status: PENDING
    depends_on: [V6T-2]
    quality_targets:
      frontend: 80
    punch_list:
      - "Market tab has TV column with 1D chips"
      - "Watchlist shows Refresh TV signals button (Sync to TV removed)"
      - "Mini chips appear on refresh"

  - id: V6T-5
    title: "Quality gate + post-chunk sync"
    status: PENDING
    depends_on: [V6T-3, V6T-4]
    quality_targets:
      code: 80
    punch_list:
      - "All 7 quality dims >=80"
      - "npm run build succeeds"
      - "forge-ship.sh V6T-5 committed and pushed"


  # --- V7 ETF + Global + Gold RS -------------------------------------------------

  - id: V7-0
    title: "Gold RS foundation — atlas_gold_rs_cache migration + GoldRSService + 6 unit tests"
    status: PENDING
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "atlas_gold_rs_cache table exists with natural-key UNIQUE + 3 indexes"
      - "Alembic upgrade/downgrade idempotent"
      - "GoldRSService computes Decimal-only rs_vs_gold_{1m,3m,6m,12m} with null-on-missing and STALE logic"
      - "Redis 15-min cache read-through"
      - "all 6 unit tests pass"
      - "post-chunk.sh completes 5 phases"

  - id: V7-1
    title: "ETF universe API — /api/etf/universe + DISTINCT ON helper + service"
    status: PENDING
    depends_on: [V7-0]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "GET /api/etf/universe?country=US returns >=100 rows with Decimal rs, technicals, optional gold_rs block"
      - "DISTINCT ON wraps every de_etf_technical_daily/de_rs_scores read"
      - "spec §17/§18/§20 green via check-api-standard.py"
      - "error envelope uniform with ETF_NOT_FOUND, INVALID_INCLUDE, JIP_UNAVAILABLE"
      - "opt-in gold_rs leaves default response shape-stable"

  - id: V7-2
    title: "ETF detail + chart-data + rs-history routes"
    status: PENDING
    depends_on: [V7-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "GET /api/etf/{ticker} returns Decimal-typed summary+rs+technicals+gold_rs"
      - "/chart-data returns ~252 rows for default 1y"
      - "/rs-history?months=12 returns ~252 points"
      - "INVALID_DATE_RANGE + DATE_RANGE_TOO_LARGE errors"
      - "all DISTINCT ON wrapped"
      - "check-api-standard.py exit 0"

  - id: V7-3
    title: "Global routes — /api/global/ratios + /rs-heatmap + /indices with four_bench_verdict"
    status: PENDING
    depends_on: [V7-0]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "/api/global/ratios returns exactly 9 series each with 10-pt sparkline + MoM change"
      - "/rs-heatmap total=131 grouped by instrument_type"
      - "/indices every row has four_bench_verdict (STRONG_BUY|BUY|HOLD|CAUTION|AVOID) and gold_rs_signal from 5-enum"
      - "nulls count as non-positive in verdict derivation"
      - "check-api-standard.py exit 0"

  - id: V7-7
    title: "Integration gate — check-api-standard.py + backend 7-dim gate + final sync"
    status: PENDING
    depends_on: [V7-2, V7-3]
    quality_targets:
      code: 80
      devops: 80
    punch_list:
      - "check-api-standard.py exits 0 across all 7 new routes"
      - "integration suite hits real DB snapshot without mocks"
      - "7-dim gate >=80 each dim (frontend dim n/a pass for backend-only slice)"
      - "Decimal-only grep clean in gold_rs_service / etf_service / global_service"
      - "post-chunk.sh final sync green"


  # --- v11-sanitization -------------------------------------------------

  - id: V11-0
    title: "Routine visibility — /api/system/routines + /forge/routines page"
    status: PENDING
    quality_targets:
      api: 85
      architecture: 85
      code: 80
      frontend: 80
    punch_list:
      - "GET /api/system/routines returns one entry per routine declared in jip-source-manifest.yaml, with status and sla_breached fields"
      - "killing a routine on jsl-wealth surfaces it red on /forge/routines within 60s of refresh"
      - "structured log on every status transition"
      - "7-dim gate ≥80 with code/api/observability watch-points"

  - id: V11-1
    title: "Manifest + health-check wiring + scoring calibration"
    status: PENDING
    depends_on: [V11-0]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
      devops: 80
      docs: 80
      frontend: 80
    punch_list:
      - "scripts/check-data-coverage.py --strict --mandatory-only runs in CI and exits non-zero on any failing mandatory domain"
      - "/forge/data-health renders one card per domain with six dimension badges"
      - "deleting the latest partition row of one domain drops only its freshness dimension below floor on the next run"
      - "year-partitioned tables score correctly against the latest live partition"
      - "integrity sampling completes in <5s on tables ≥1M rows"
      - "health_gate('domain') dependency raises 503 with structured detail when domain failing"
      - "7-dim gate ≥80 with correctness/determinism/tests watch-points"

  - id: V11-2
    title: "Adjustment-factor consumption — ?adjusted=true on price routes"
    status: PENDING
    depends_on: [V11-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "GET /api/instruments/RELIANCE/price returns adjusted series by default that is continuous across known bonus dates"
      - "?adjusted=false returns raw with the artificial jump preserved"
      - "comparison matches manual computation from de_corporate_actions"
      - "when adjustment-factor health fails, route returns raw + meta.warnings naming the failing dimension"
      - "no float in adjustment math"
      - "7-dim gate ≥80 with correctness/standards watch-points"

  - id: V11-3
    title: "Gold/USD denomination lens — ?denomination=inr|gold|usd"
    status: PENDING
    depends_on: [V11-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "GET /api/instruments/NIFTY50/price?denomination=gold returns series equal to inr-series divided by gold-rupee series indexed to common base"
      - "matches manual computation to 4 decimals"
      - "second call within TTL hits cache and returns within 50ms p50 (benchmark in test)"
      - "meta.data_as_of propagates worst-of staleness"
      - "7-dim gate ≥80 with correctness/tests watch-points"

  - id: V11-4
    title: "Derivatives + India VIX consumption routes (gated on JIP)"
    status: PENDING
    depends_on: [V11-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "When de_fo_bhavcopy_daily and de_fo_participant_oi_daily are healthy: GET /api/derivatives/pcr/NIFTY returns a value matching the NSE-published PCR for the same date to 2 decimals"
      - "GET /api/derivatives/{symbol}/oi returns OI buildup chart payload"
      - "GET /api/macros/vix returns the VIX time series"
      - "When any underlying JIP table is unhealthy: routes return 503 with detail.reason naming failing dimension and structured warn log emitted"
      - "7-dim gate ≥80 with correctness/observability watch-points"

  - id: V11-5
    title: "empyrical adoption — replace hand-rolled risk metrics"
    status: PENDING
    depends_on: [V11-1]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "test_analytics_parity passes — 5 fixture series × 8 metrics = 40 assertions all agree to 4 decimals between legacy and empyrical"
      - "hand-rolled risk math is deleted from analytics.py"
      - "all simulation endpoints return identical numbers vs pre-migration baseline on a smoke run"
      - "no float leaks past the service boundary (mypy + custom ruff check)"
      - "7-dim gate ≥80 with correctness/tests watch-points"

  - id: V11-6
    title: "vectorbt port of backtest engine"
    status: PENDING
    depends_on: [V11-5]
    quality_targets:
      architecture: 85
      code: 80
    punch_list:
      - "test_backtest_parity passes — 5 reference simulations × 12 output metrics = 60 assertions agree to 4 decimals between legacy and vectorbt"
      - "test_backtest_perf shows wall-clock for a 100-config parameter sweep on vectorbt is ≤1/10 of legacy"
      - "?engine=legacy still works one chunk for rollback safety"
      - "7-dim gate ≥80 with correctness/performance/tests watch-points"

  - id: V11-7
    title: "Macro routes — yield curve + FX + RBI policy events (gated on JIP)"
    status: PENDING
    depends_on: [V11-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "When de_in_gsec_yields_daily, de_fx_rates_daily, de_rbi_policy_rates are healthy: yield-curve points match CCIL-published values for one known date to 0.01bp"
      - "FX rates match RBI reference rates"
      - "policy events list matches RBI press releases"
      - "When any JIP table fails: 503 with detail.reason"
      - "7-dim gate ≥80 with correctness/api/observability watch-points"

  - id: V11-8
    title: "Insider + bulk/block deal surfaces on stock detail (gated on JIP)"
    status: PENDING
    depends_on: [V11-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "When de_insider_trades, de_bulk_deals, de_block_deals are healthy: GET /api/stocks/{symbol}/insider returns rows matching SEBI PIT disclosure for one known event"
      - "bulk-deals and block-deals match NSE archive for one known date"
      - "When any JIP table fails: 503 with detail.reason"
      - "7-dim gate ≥80 with correctness/api watch-points"

  - id: V11-9
    title: "OpenBB + FinanceToolkit pilot on /api/stocks/{symbol}/analysis"
    status: PENDING
    depends_on: [V11-1]
    quality_targets:
      api: 85
      architecture: 85
      code: 80
    punch_list:
      - "?engine=openbb returns a strict superset of the legacy structured-signal keys (schema-diff test)"
      - "on a 100-call sequential benchmark p95 of openbb is ≤1.5x p95 of legacy"
      - "pilot disabled by default (legacy still default)"
      - "7-dim gate ≥80 with correctness/performance/api watch-points"

  - id: V11-10
    title: "Coverage close-out — strict gate green across every domain"
    status: PENDING
    depends_on: [V11-0, V11-1, V11-2, V11-3, V11-4, V11-5, V11-6, V11-7, V11-8, V11-9]
    quality_targets:
      code: 80
      devops: 80
      docs: 80
    punch_list:
      - "scripts/check-data-coverage.py --strict --mandatory-only exits 0"
      - "report shows every mandatory domain at ≥85 weighted overall and every dimension at ≥80"
      - "three consecutive runs produce identical scores (determinism)"
      - "CI strict-coverage workflow is gating PRs"
      - "7-dim gate ≥80 with determinism/standards watch-points"


  # --- V1FE -------------------------------------------------

  - id: V1FE-1
    title: "Gate runner: scripts/check-frontend-criteria.py + fe_checks lib + .quality dim"
    status: PENDING
    quality_targets:
      code: 80
      devops: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --list-types lists 28 types"
      - "runner preflight rejects unknown check types"
      - ".forge/frontend-report.json matches contracts/runner-report-format.md schema"
      - "results[] sorted by id"
      - "two consecutive runs byte-identical (modulo generated_at in --report-only)"
      - "exit code follows critical==0 AND high==0-or-exempted contract"
      - ".quality/checks.py --dim frontend returns numeric score"
      - "≥19 unit tests green covering every check type"

  - id: V1FE-2
    title: "Component library: components.html + tokens.css v1.1 + 4 new fixtures"
    status: PENDING
    depends_on: [V1FE-1]
    quality_targets:
      code: 80
      devops: 80
      docs: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --only 'fe-dp-*,fe-f-03,fe-f-04,fe-f-05,fe-f-06,fe-f-07,fe-f-08,fe-f-09,fe-f-10' exits 0"
      - "html5validator frontend/mockups/components.html exits 0"
      - "tokens.css additive diff only (no removed keys)"
      - "4 new fixtures validate against Draft-07 schemas"
      - "mf_rank composite matches round((r+k+s+c)/4, 1) to 0.1 precision on 3 sampled rows"
      - "re-running each seed script with same --as-of produces byte-identical JSON"
      - "sector_rrg tail length ≥8 per sector"
      - "≥8 unit tests green"

  - id: V1FE-3
    title: "index.html landing + link audit"
    status: PENDING
    depends_on: [V1FE-2]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --only 'fe-l-01,fe-l-02,fe-g-*,fe-mob-*' exits 0"
      - "every <a href> resolves to a file under frontend/mockups/"
      - "zero external links"
      - "test_index_structure.py ≥3 cases green"

  - id: V1FE-4
    title: "today.html Pulse — regime banner + 4-decision grid + universal benchmarks"
    status: PENDING
    depends_on: [V1FE-2]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --only 'fe-p1-*,fe-g-*,fe-dp-*,fe-mob-*,fe-state-*,fe-c-*' exits 0"
      - "every instrument row has exactly 7 chips in canonical order"
      - "rec-slot pulse-regime present"
      - "kill-list clean"
      - "Indian formatting everywhere"
      - "360px no hscroll"

  - id: V1FE-5
    title: "explore-global.html — macros/rates/FX/commodities/credit/risk + global sector RRG"
    status: PENDING
    depends_on: [V1FE-2]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --only 'fe-p4-*,fe-g-*,fe-dp-*,fe-mob-*,fe-state-*,fe-c-*' exits 0"
      - "kill-list forbids 'risk-on verdict'/'risk-off verdict'"
      - "rec-slot global-regime present"

  - id: V1FE-6
    title: "explore-country.html — India regime + breadth panel + FII/DII + compact §10.5"
    status: PENDING
    depends_on: [V1FE-2]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --only 'fe-p5-*,fe-g-*,fe-dp-*,fe-mob-*,fe-state-*,fe-c-*' exits 0"
      - "breadth panel renders three fixed zone bands per §1.11"
      - "rec-slot country-breadth present"

  - id: V1FE-7
    title: "explore-sector.html — dual-scope regime + members grid + full §10.5 + RRG"
    status: PENDING
    depends_on: [V1FE-2]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --only 'fe-p6-*,fe-g-*,fe-dp-*,fe-mob-*,fe-state-*,fe-c-*' exits 0"
      - "3 rec-slots present"
      - "every member row has 7 chips in canonical order"

  - id: V1FE-8
    title: "stock-detail.html — hub-and-spoke hero + 7 tabs + divergences + §10.5 compact"
    status: PENDING
    depends_on: [V1FE-2]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --only 'fe-p7-*,fe-g-*,fe-dp-*,fe-mob-*,fe-state-*,fe-c-*' exits 0"
      - "3 rec-slots present"
      - "7 tabs present"
      - "kill-list clean"
      - "simulate-this component present"

  - id: V1FE-9
    title: "mf-detail.html — hub-and-spoke hero + 7 tabs + suitability stripped + §10.5"
    status: PENDING
    depends_on: [V1FE-2]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --only 'fe-p8-*,fe-g-*,fe-dp-*,fe-mob-*,fe-state-*,fe-c-*' exits 0"
      - "suitability block contains zero kill-list tokens including 'HOLD / ADD ON DIPS', 'ADD ON DIPS', 'Atlas Verdict'"
      - "2 rec-slots present"
      - "NAV chart vs category-bench TRI overlay present"

  - id: V1FE-10
    title: "mf-rank.html — 4-factor composite ranking with dimensional-fix v1.1"
    status: PENDING
    depends_on: [V1FE-2]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --only 'fe-p9-*,fe-g-*,fe-dp-*,fe-mob-*,fe-state-*,fe-c-*' exits 0"
      - "composite recomputed from *_cdf fields matches display to 0.1 on 3 sampled rows"
      - "tie-break ordering applied correctly when composites match"
      - "rec-slot mfrank-screens present"
      - "formula disclosure block present with plain-English + mathematical + source citation"

  - id: V1FE-11
    title: "breadth.html — canonical Breadth Terminal with full §10.5 embed"
    status: PENDING
    depends_on: [V1FE-2]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --only 'fe-p10-*,fe-p10_5-*,fe-g-*,fe-dp-*,fe-mob-*,fe-state-*,fe-c-*' exits 0"
      - "all 14 §10.5 DOM ids present (#i_initial..#i_l_sip_resume)"
      - "zone classification matches fixture zone field 100%"
      - "3 rec-slots present"
      - "zone-event dots at every zone transition in fixture"

  - id: V1FE-12
    title: "portfolios.html — 4 books, accountability banner stripped, per-book rec-slots"
    status: PENDING
    depends_on: [V1FE-2]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --only 'fe-p11-*,fe-g-*,fe-dp-*,fe-mob-*,fe-state-*,fe-c-*' exits 0"
      - "accountability-banner + pending-rec ledger DOM selectors absent"
      - "4 rec-slots present (portfolio-book-1..4)"
      - "each slot carries all 3 data-* attributes"

  - id: V1FE-13
    title: "lab.html — 3 mode tabs, full §10.5 Breadth Playback, greyed V1.1 rule backtest"
    status: PENDING
    depends_on: [V1FE-11]
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py --only 'fe-p12-*,fe-p10_5-*,fe-g-*,fe-dp-*,fe-mob-*,fe-state-*,fe-c-*' exits 0"
      - "all 3 mode tabs present"
      - "rule-backtest tab aria-disabled=true"
      - "14 §10.5 DOM ids present in breadth-playback mode"
      - "2 rec-slots present"

  - id: V1FE-14
    title: "QA sweep — Playwright baselines + a11y + full gate + smoke probe"
    status: PENDING
    depends_on: [V1FE-1, V1FE-2, V1FE-3, V1FE-4, V1FE-5, V1FE-6, V1FE-7, V1FE-8, V1FE-9, V1FE-10, V1FE-11, V1FE-12, V1FE-13]
    quality_targets:
      code: 80
      devops: 80
      frontend: 80
    punch_list:
      - "python scripts/check-frontend-criteria.py exits 0 with critical_fail_count==0 across all ~106 criteria"
      - "python scripts/check-fe-a11y.py exits 0"
      - "pytest tests/e2e/fe_pages/ -v green"
      - "50 baseline PNGs committed + bit-identical on rerun"
      - ".quality/checks.py --dim frontend ≥ 95"
      - "curl -sf https://atlas.jslwealth.in/mockups/<page> | grep -q ATLAS green for every page"


  # --- V2FE — Stage 2 Data Wiring -------------------------------------------------
  # V2FE-0 and V2FE-1 run in parallel (no inter-dependency).
  # V2FE-2..V2FE-7 are per-page and run in parallel once V2FE-0 + V2FE-1 are done.
  # V2FE-8 (states rollout) follows all per-page chunks.
  # V2FE-9 (integration gate) is the slice DONE signal.
  # Spec: docs/design/frontend-v2-spec.md §9

  - id: V2FE-0
    title: "Criteria YAML + loader skeleton + §6 states contract"
    status: PENDING
    spec: docs/specs/chunks/V2FE-0.md
    depends_on: []
    estimated_hours: 4
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "docs/specs/frontend-v2-criteria.yaml exists and parses without error (≥30 entries)"
      - "scripts/check-frontend-v2.py --list prints ≥9 backend check IDs"
      - "frontend/mockups/assets/atlas-data.js exports loadBlock, fetchWithTimeout, buildUrl, hasData, isStale"
      - "frontend/mockups/assets/atlas-states.js exports STALENESS_THRESHOLDS keyed per §6.3 data class"
      - "STALENESS_THRESHOLDS values match §6.3 exactly (intraday=3600, eod_breadth=21600, daily_regime=86400, fundamentals=604800, events=604800, holdings=604800, system=21600)"
      - "pytest tests/unit/v2fe/test_atlas_data_js.py -v passes ≥8 tests"
      - "scripts/check-frontend-criteria.py exits 0 (V1 gate unchanged)"

  - id: V2FE-1a
    title: "Backend: zone-events + breadth divergences endpoints"
    status: PENDING
    spec: docs/specs/chunks/V2FE-1a.md
    depends_on: []
    estimated_hours: 3
    quality_targets:
      api: 85
      code: 80
      test: 80
    punch_list:
      - "GET /api/v1/stocks/breadth/zone-events returns 200 with _meta envelope"
      - "GET /api/v1/stocks/breadth/divergences returns 200 with _meta envelope"
      - "Both return data:[] (not error) when JIP sparse"
  - id: V2FE-1b
    title: "Backend: global events + flows + atlas_key_events migration"
    status: PENDING
    spec: docs/specs/chunks/V2FE-1b.md
    depends_on: [V2FE-1a]
    estimated_hours: 3
    quality_targets:
      api: 85
      code: 80
      test: 80
    punch_list:
      - "GET /api/v1/global/events returns 200 with _meta envelope"
      - "GET /api/v1/global/flows returns 200 with crore totals + daily series"
      - "alembic upgrade head exits 0"
  - id: V2FE-1c
    title: "Backend: UQL templates + conviction_series include extension"
    status: PENDING
    spec: docs/specs/chunks/V2FE-1c.md
    depends_on: [V2FE-1b]
    estimated_hours: 3
    quality_targets:
      api: 85
      code: 80
      test: 80
    punch_list:
      - "POST /query/template with stock_peers template returns 200"
      - "POST /query/template with mf_rank_composite template returns 200"
      - "GET /mf/{id}/weighted-technicals?include=conviction_series includes conviction_series"
  - id: V2FE-1
    title: "Backend gaps: zone-events + global-events + divergences + flows + UQL templates + conviction_series"
    status: PENDING
    spec: docs/specs/chunks/V2FE-1.md
    depends_on: []
    estimated_hours: 10
    quality_targets:
      api: 85
      architecture: 85
      code: 80
      security: 85
    punch_list:
      - "GET /api/v1/stocks/breadth/zone-events?universe=nifty500&range=5y returns 200 with zone_events.schema.json-valid payload"
      - "GET /api/v1/global/events?scope=india,global returns 200 with events.schema.json-valid payload"
      - "GET /api/v1/stocks/breadth/divergences?universe=nifty500 returns 200 with divergences[] array and _meta.insufficient_data field"
      - "GET /api/v1/global/flows returns 200 or empty + insufficient_data:true when de_fii_dii_daily empty"
      - "POST /api/v1/query/template {template:'sector_rotation'} returns ≥11 rows with sector_id, rs, rs_gold, conviction"
      - "POST /api/v1/query/template {template:'top_rs_gainers', params:{limit:10}} returns exactly 10 rows ordered desc by delta_pct"
      - "POST /api/v1/query/template {template:'fund_1d_movers', params:{limit:5}} returns 5 rows with rs_composite + gold_rs_state"
      - "POST /api/v1/query/template {template:'mf_rank_composite'} rows parse against mf_rank_universe.schema.json"
      - "GET /api/v1/stocks/breadth?include=conviction_series returns conviction_series field in response"
      - "Every new response carries _meta envelope per §6.4 with data_as_of, source, staleness_seconds, includes_loaded"
      - "alembic upgrade head and alembic downgrade -1 both succeed for atlas_key_events migration"
      - "No float in any new service file (grep clean)"
      - "scripts/check-api-standard.py exits 0 for all new routes"
      - "pytest tests/services/test_breadth_zone_detector.py tests/services/test_breadth_divergence_detector.py tests/services/test_event_marker_service.py tests/services/test_flows_service.py -v green"

  - id: V2FE-2
    title: "Today / Pulse page data wiring"
    status: PENDING
    spec: docs/specs/chunks/V2FE-2.md
    depends_on: [V2FE-0, V2FE-1c]
    estimated_hours: 4
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "grep -c 'data-endpoint' today.html returns ≥8"
      - "pytest tests/unit/v2fe/test_today_bindings.py -v passes ≥6 tests"
      - "scripts/check-frontend-criteria.py --only 'fe-p1-*,fe-g-*,fe-dp-*,fe-mob-*' exits 0"
      - "Four-decision-card slots carry data-v2-deferred='true' (not data-endpoint)"
      - "No data-endpoint value points at a URL outside §4.1 or §4.2"
      - "atlas-data.js and atlas-states.js script tags present in today.html"

  - id: V2FE-3
    title: "Explore · Country (India deep dive) page data wiring"
    status: PENDING
    spec: docs/specs/chunks/V2FE-3.md
    depends_on: [V2FE-0, V2FE-1c]
    estimated_hours: 4
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "grep -c 'data-endpoint' explore-country.html returns ≥10"
      - "pytest tests/unit/v2fe/test_explore_country_bindings.py -v passes ≥6 tests"
      - "scripts/check-frontend-criteria.py --only 'fe-p5-*,fe-g-*,fe-dp-*,fe-mob-*' exits 0"
      - "Derivatives block carries data-sparse='true' (de_fo_bhavcopy 0 rows)"
      - "INR chart block carries data-sparse='true' (USDINR 3 rows)"
      - "signal-history-table binds to /api/v1/stocks/breadth/zone-events"

  - id: V2FE-4
    title: "Breadth Terminal page data wiring"
    status: PENDING
    spec: docs/specs/chunks/V2FE-4.md
    depends_on: [V2FE-0, V2FE-1c]
    estimated_hours: 4
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "grep -c 'data-endpoint' breadth.html returns ≥9"
      - "pytest tests/unit/v2fe/test_breadth_bindings.py -v passes ≥8 tests"
      - "scripts/check-frontend-criteria.py --only 'fe-p10-*,fe-p10_5-*,fe-g-*,fe-dp-*,fe-mob-*' exits 0"
      - "[data-block=signal-history] carries data-endpoint='/api/v1/stocks/breadth/zone-events'"
      - "Conviction halo block binds to breadth endpoint with include=conviction_series"
      - "Universe-selector pill change re-fires loadBlock on universe-dependent blocks"
      - "14 data-param-id sentinel inputs unchanged from V1FE-11"

  - id: V2FE-5
    title: "Stock detail (hub-and-spoke equity terminal) page data wiring"
    status: PENDING
    spec: docs/specs/chunks/V2FE-5.md
    depends_on: [V2FE-0, V2FE-1c]
    estimated_hours: 4
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "grep -c 'data-endpoint' stock-detail.html returns ≥12"
      - "pytest tests/unit/v2fe/test_stock_detail_bindings.py -v passes ≥8 tests"
      - "scripts/check-frontend-criteria.py --only 'fe-p7-*,fe-g-*,fe-dp-*,fe-mob-*' exits 0"
      - "[data-block=peers] carries data-endpoint='/api/v1/query' with entity_type=equity in data-params"
      - "3 rec-slots carry data-v2-deferred='true'"
      - "simulate-this affordance links to lab.html?symbol=... (not a backend endpoint)"
      - "All 7 tabs and 3 rec-slots DOM contract from V1FE-8 intact"

  - id: V2FE-6
    title: "MF detail (hub-and-spoke MF terminal) page data wiring"
    status: PENDING
    spec: docs/specs/chunks/V2FE-6.md
    depends_on: [V2FE-0, V2FE-1c]
    estimated_hours: 4
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "grep -c 'data-endpoint' mf-detail.html returns ≥12"
      - "pytest tests/unit/v2fe/test_mf_detail_bindings.py -v passes ≥8 tests"
      - "scripts/check-frontend-criteria.py --only 'fe-p8-*,fe-g-*,fe-dp-*,fe-mob-*' exits 0"
      - "[data-block=returns] carries data-endpoint pointing to /api/v1/mf/{id}/nav-history with include=rolling_returns"
      - "[data-block=peers] carries data-endpoint='/api/v1/query' with entity_type=mutual_fund"
      - "2 rec-slots carry data-v2-deferred='true'"
      - "V1FE-9 kill-list tokens absent (Atlas Verdict, HOLD / ADD ON DIPS)"

  - id: V2FE-7
    title: "MF rank (4-factor composite ranking) page data wiring"
    status: PENDING
    spec: docs/specs/chunks/V2FE-7.md
    depends_on: [V2FE-0, V2FE-1c]
    estimated_hours: 3
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "grep -c 'data-endpoint' mf-rank.html returns ≥5"
      - "pytest tests/unit/v2fe/test_mf_rank_bindings.py -v passes ≥6 tests"
      - "scripts/check-frontend-criteria.py --only 'fe-p9-*,fe-g-*,fe-dp-*,fe-mob-*' exits 0"
      - "[data-block=rank-table] carries data-endpoint='/api/v1/query/template' with template=mf_rank_composite in data-params"
      - "footer.methodology-footer carries data-endpoint='/api/v1/system/data-health'"
      - "[data-role=formula] has no data-endpoint (whitelisted as static)"
      - "Sparkline loading uses single batched template call (not N individual calls)"

  - id: V2FE-8
    title: "States rollout: loading/empty/stale/error across all 6 pages + staleness banners"
    status: PENDING
    spec: docs/specs/chunks/V2FE-8.md
    depends_on: [V2FE-2, V2FE-3, V2FE-4, V2FE-5, V2FE-6, V2FE-7]
    estimated_hours: 3
    quality_targets:
      code: 80
      frontend: 80
    punch_list:
      - "pytest tests/unit/v2fe/test_states_contract.py -v passes ≥10 tests"
      - "atlas-data.js contains 10s hard cut-off setTimeout guard for loading state"
      - "atlas-data.js contains ?sim_state= dev-mode support gated to localhost"
      - "renderSkeleton, renderEmpty, renderStaleBanner, renderError all verified on at least one block per page"
      - "Known-sparse blocks (data-sparse='true') render empty-state on insufficient_data:true (not error)"
      - "data-as-of attribute updated from _meta.data_as_of on every successful loadBlock"
      - "scripts/check-frontend-criteria.py exits 0 for all 6 pages (no V1 regression)"

  - id: V2FE-9
    title: "Integration gate: Playwright E2E + Lighthouse regression + API-standard + spec-coverage"
    status: PENDING
    spec: docs/specs/chunks/V2FE-9.md
    depends_on: [V2FE-8]
    estimated_hours: 3
    quality_targets:
      code: 80
      frontend: 95
    punch_list:
      - "npx playwright test tests/e2e/v2fe_*.spec.ts exits 0 (7 spec files: 1 backend + 6 page)"
      - ".forge/v2fe-lighthouse-report.json exists with LCP < 2.5s and CLS < 0.1 for all 6 pages"
      - "Deterministic replay: two consecutive loads return identical _meta.data_as_of and records (modulo cache_hit)"
      - "scripts/check-api-standard.py exits 0"
      - "scripts/check-spec-coverage.py exits 0 (frontend-v2-criteria.yaml registered + linked to spec §15)"
      - "scripts/check-frontend-criteria.py exits 0 with critical_fail_count==0"
      - "scripts/check-frontend-v2.py exits 0"
      - "python .quality/checks.py frontend dim ≥ 95"
