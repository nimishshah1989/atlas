---
id: S1-7
title: "Navigation + root redirect + build gate"
status: PENDING
estimated_hours: 2
depends_on: [S1-1, S1-2, S1-3, S1-4, S1-5, S1-6]
gate_criteria:
  code: 80
  fe: 80
---

## Objective

Add a persistent top navigation bar to the app layout that links to all 6 live
pages. Redirect `/` to `/pulse`. Run `npm run build` as the final integration gate
for the S1 sprint — it must exit 0 with no TypeScript errors and no route conflicts.

## Punch list

- [ ] Read `frontend/mockups/_nav-shell.html` in full — use it as the canonical reference
      for nav bar structure, link labels, and active state styling.
- [ ] Read `frontend/src/app/layout.tsx` before modifying it.
- [ ] Read `frontend/src/app/page.tsx` before modifying it.
- [ ] Read `node_modules/next/dist/docs/` for Next.js 16 App Router `layout.tsx`,
      `redirect()`, and `usePathname()` conventions before writing any layout code.
- [ ] Update `frontend/src/app/layout.tsx`:
  - Import and render `<TopNav />` above `{children}` in the root layout.
  - `TopNav` must be rendered server-side compatible — do NOT use `"use client"` on layout.
  - Pass nav items as static config to `TopNav`.
- [ ] Create `frontend/src/components/nav/TopNav.tsx` — `"use client"` (needs `usePathname`):
  - Links:
    - "Pulse" → `/pulse` (maps to today.html)
    - "India" → `/explore/country` (maps to explore-country.html)
    - "Breadth" → `/breadth` (maps to breadth.html)
    - "Stocks" → `/stocks/HDFCBANK` (default symbol, hub-and-spoke entry)
    - "Funds" → `/funds/rank` (maps to mf-rank.html)
    - "Fund Detail" → `/funds/ppfas-flexi-cap-direct-growth` (maps to mf-detail.html)
  - Active link: highlight current page using `usePathname()` — matches `--accent-700`
    border-bottom style from _nav-shell.html `topbar__link--active`.
  - Design tokens: use `--bg-surface`, `--border-default`, `--accent-700`,
    `--text-primary`, `--text-secondary` CSS variables (not Tailwind colours directly,
    to match design token system from tokens.css).
  - Sticky: `position: sticky; top: 0; z-index: 30` (matches .topbar in mockups).
  - Atlas logo / wordmark on the left — use text "ATLAS" in serif font (`--font-serif`).
  - Mobile: collapse to hamburger menu below `md` breakpoint (Tailwind `hidden md:flex`
    for link row; hamburger icon shown below `md`).
- [ ] Update `frontend/src/app/page.tsx`:
  - Replace any existing content with a Next.js `redirect("/pulse")`.
  - Use `import { redirect } from "next/navigation"` (App Router pattern).
  - This is a server component — no `"use client"` directive.
- [ ] Verify no route conflicts: `frontend/src/app/` should have these routes after S1:
  - `/` → redirect to `/pulse`
  - `/pulse/` → S1-1
  - `/explore/country/` → S1-2
  - `/breadth/` → S1-3
  - `/stocks/[symbol]/` → S1-4
  - `/funds/[id]/` → S1-5
  - `/funds/rank/` → S1-6
  - `/forge/` → existing (do NOT modify)
  - `/pro/*` → existing (do NOT modify)
  - NOTE: `/funds/rank` and `/funds/[id]` must not conflict — Next.js resolves static segments
    before dynamic params. Verify by reading `node_modules/next/dist/docs/` routing docs.
- [ ] Write `tests/unit/nav/test_top_nav.test.tsx` — at least 5 tests:
  - TopNav renders all 6 nav links.
  - TopNav marks `/pulse` as active when pathname is "/pulse".
  - TopNav marks `/breadth` as active when pathname is "/breadth".
  - Root page redirects to /pulse (test the redirect() call).
  - TopNav has sticky positioning class.
- [ ] Run `cd /home/ubuntu/atlas/frontend && npm test -- --passWithNoTests` — must exit 0.
- [ ] Run `cd /home/ubuntu/atlas/frontend && npm run build` — must exit 0. This is the
      primary gate for the entire S1 sprint. Build failures from any earlier S1 chunk are
      surfaced here and must be fixed.
- [ ] Smoke check: confirm `/pulse` loads without TypeScript errors by checking build output.

## Exit criteria (measurable)

- `frontend/src/app/layout.tsx` renders `<TopNav />`.
- `frontend/src/components/nav/TopNav.tsx` exists with all 6 links.
- `frontend/src/app/page.tsx` redirects to `/pulse`.
- All 5+ unit tests pass.
- `cd frontend && npm run build` exits 0 — **no TypeScript errors, no route conflicts**.
- `cd frontend && npm test` exits 0.
- `/funds/rank` and `/funds/[id]` routing does not conflict (static before dynamic).

## Domain constraints

- Read `frontend/mockups/_nav-shell.html` before writing TopNav.
- Read `node_modules/next/dist/docs/` for App Router layout and redirect conventions.
- Do NOT modify any existing `/forge` or `/pro/*` routes — they must remain functional.
- TopNav must use CSS variables from tokens.css — not hardcoded hex colours. This ensures
  design token consistency across the entire app.
- The redirect in `page.tsx` must use Next.js `redirect()` from `"next/navigation"` —
  not `useRouter().push()` (which requires client component) and not `<meta http-equiv="refresh">`.
- Mobile hamburger: menu state via `React.useState<boolean>(false)`. No external library.
- Do NOT install any new npm packages in this chunk. All dependencies already available.
- `npm run build` failure in this chunk means a prior S1 chunk shipped broken code.
  Fix the root cause in the affected component — do not suppress TypeScript errors with `// @ts-ignore`.
