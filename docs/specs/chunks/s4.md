# Chunk S4 — CLAUDE.md surgery (strip to ~80 lines, operational rules only)

**Depends on:** S3
**Blocks:** V1.6 R1 (first V1.6 chunk reads the stripped CLAUDE.md during Step 0 boot)
**Complexity:** S (1h)
**Dimension:** architecture (the `claude_md_present_and_live` check S2 folded in)

---

## Goal

Strip `/home/ubuntu/atlas/CLAUDE.md` from its current ~356 lines down to **~80 lines of operational rules only**. Everything scope-related moves to the spec (which is sacrosanct) or to dedicated doc files. CLAUDE.md becomes a rulebook, not an encyclopedia — the thing every chunk session reads at Step 0 boot, so it has to be fast to read and impossible to drift from.

## Why

The user's instruction, verbatim: "CLAUDE.md is bloated. Strip it." Current file contains V1 completion criteria that have already drifted from the spec (criterion count is 13, spec §24.3 has 15), a full Build Order V1→V10, a Critical Schema Facts table, a Data Flow section listing which tables come from where, a Technology Stack fork/install/build breakdown, an ATLAS-OWNS tables list. Every one of those is shadow copy of content that lives authoritatively elsewhere and rots the moment the source of truth changes. The FD chunks were the canary — they ran with a CLAUDE.md that said "V1 criteria" but the actual criteria were in the spec, and nobody noticed they differed.

## Files

### Modified
- `/home/ubuntu/atlas/CLAUDE.md` — full rewrite, target 80–100 lines.

### New
- `docs/architecture/critical-schema-facts.md` — moved from CLAUDE.md. The "spec v2 was wrong" table lives here now.
- `docs/architecture/data-flow.md` — moved from CLAUDE.md. What comes from JIP, what ATLAS computes, what ATLAS owns.
- `docs/architecture/tech-stack.md` — moved from CLAUDE.md. Fork / pip-install / build-in-house breakdown.

### Unchanged
- `ATLAS-DEFINITIVE-SPEC.md` — sacrosanct. S4 does not touch it.
- `docs/specs/v1-criteria.yaml` — S3 owns it. CLAUDE.md now points to it as the V1 criteria source of truth.

## New CLAUDE.md shape (target ~80 lines)

```markdown
# ATLAS — Market Intelligence Engine

Operational rulebook. Not scope. Scope lives in ATLAS-DEFINITIVE-SPEC.md.

## Four Laws (non-negotiable)
1. Prove, never claim
2. No synthetic data
3. Backend first always
4. See what you build

## System Guarantees (non-negotiable)
1. Deterministic  2. Explainable  3. Fault-tolerant  4. Traceable  5. Idempotent

## Project conventions
- Financial values: Decimal, never float. Paise internal, rupees at API boundary
- Indian formatting: lakh/crore
- Dates: IST timezone aware
- API: FastAPI + Pydantic v2, async endpoints
- DB: SQLAlchemy 2.0 async, Alembic migrations, no raw DDL
- Logging: structlog, no print()

## Test commands
- pytest tests/ -v --tb=short
- ruff check . --select E,F,W
- mypy . --ignore-missing-imports
- cd frontend && npm test

## Source-of-truth pointers
- Full spec (sacrosanct): ATLAS-DEFINITIVE-SPEC.md
- V1 completion criteria: docs/specs/v1-criteria.yaml
- Critical schema facts: docs/architecture/critical-schema-facts.md
- Data flow (JIP → compute → own): docs/architecture/data-flow.md
- Tech stack (fork/install/build): docs/architecture/tech-stack.md
- Chunk ledger: orchestrator/plan.yaml
- Chunk specs: docs/specs/chunks/
- Quality rubric: .quality/standards.md

## Post-chunk sync invariant
Every chunk reaching DONE must leave git, EC2, and wiki in sync via
scripts/post-chunk.sh. Residual commit → push → service restart →
/forge-compile → memory sync. A chunk is not DONE until git, EC2, wiki,
and MEMORY.md all agree.

## Chunk boot-context protocol (Step 0)
Every chunk starts with a Step 0 Boot block. Before planning: read (1) this
file, (2) MEMORY.md + relevant memory files (especially
project_v15_chunk_status.md), (3) .forge/knowledge/wiki/index.md then
only the relevant wiki articles, (4) only the spec sections the punch
list touches. A chunk that skips Step 0 is operating on stale context.

## Context discipline
- One chunk per session. Fresh context per chunk.
- Subagents: context: fork. Main agent sees summaries only.
- Do NOT accumulate state across chunks.
```

Target: ~80 lines of pure operational discipline. Zero scope content. Zero copy of the spec.

## Punch list

1. Read the current CLAUDE.md end-to-end; identify the content that must move vs. the content that is already pure operational discipline.
2. Create `docs/architecture/` directory if it doesn't exist.
3. Move Critical Schema Facts → `docs/architecture/critical-schema-facts.md` verbatim. Add a one-line pointer in CLAUDE.md.
4. Move Data Flow section → `docs/architecture/data-flow.md`. Same.
5. Move Technology Stack → `docs/architecture/tech-stack.md`. Same.
6. Delete from CLAUDE.md: the V1 completion criteria list (now in `v1-criteria.yaml`), the Build Order V1→V10 (already in the spec), the Hard Stop Conditions block (belongs in `.quality/standards.md` as gate failure triggers), the ATLAS-OWNS tables list (now in `data-flow.md`), the three-service architecture diagram (now in `data-flow.md`).
7. Rewrite what remains to match the target shape above. Preserve the Post-chunk sync invariant and Chunk boot-context protocol blocks verbatim — those are load-bearing.
8. Run the `architecture` dim of the quality engine and confirm `claude_md_present_and_live` passes with the new file.
9. Verify line count: 80 ≤ lines ≤ 120. Hard cap at 120.
10. Grep-verify: no string "V1→V10", no string "Critical Schema Facts", no string "ATLAS OWNS" in the new CLAUDE.md — all should be in the moved docs.

## Completion criteria (S4)

- [ ] CLAUDE.md is 80–120 lines, operational rules only.
- [ ] Three new docs exist under `docs/architecture/` containing the moved content, verbatim where possible.
- [ ] No scope content remains in CLAUDE.md (build order, criteria list, tech stack, schema facts, data flow).
- [ ] Four Laws + System Guarantees + Post-chunk sync invariant + Chunk boot protocol preserved verbatim.
- [ ] `architecture` dim still ≥ 80 after the rewrite.
- [ ] All pointers in CLAUDE.md resolve to real files.
- [ ] Post-chunk sync hook runs clean.
- [ ] After S4 lands, all 5 gating dims still ≥ 80 and the forge dashboard renders cleanly.
