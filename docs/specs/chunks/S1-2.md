---
id: S1-2
title: "Explore Country page (explore-country.html → /explore/country)"
status: PENDING
estimated_hours: 4
depends_on: [S1-0]
gate_criteria:
  code: 80
  fe: 80
---

## Objective

Convert `frontend/mockups/explore-country.html` into a live Next.js page at
`/explore/country`. Every `data-endpoint` block becomes a React component using
`useAtlasData`. Sparse data sources (derivatives, INR chart) must use
`data-sparse="true"` equivalents that render `<EmptyState>` on
`insufficient_data: true` rather than an error.

## Punch list

- [ ] Read `frontend/mockups/explore-country.html` in full before writing any component.
- [ ] Read `frontend/mockups/assets/explore-country.js` (if it exists) for page-level
      globals or selector wiring.
- [ ] Read `node_modules/next/dist/docs/` for Next.js 16 App Router conventions
      before writing the page file.
- [ ] Read `frontend/src/components/MarketOverview.tsx` to understand existing style.
- [ ] Audit all `data-endpoint`, `data-block`, and `data-component` elements in
      `explore-country.html` — comment them at the top of `page.tsx`. Note which
      carry `data-sparse="true"` or `data-v2-derived="true"`.
- [ ] Create `frontend/src/app/explore/country/page.tsx` — `"use client"`.
  - Page title: "ATLAS · Explore India" (or as per mockup `<title>`).
  - Layout mirrors explore-country.html: regime/signal header, breadth compact section,
    derivatives section, yield curve section, INR chart section, flows section, sectors RRG section.
- [ ] Create `frontend/src/components/explore/BreadthCompactBlock.tsx`:
  - Fetches `/api/v1/stocks/breadth` with `{ universe: "nifty500", range: "5y" }` and
    fixture `fixtures/breadth_daily_5y.json`.
  - Renders 3 KPI cards (above EMA21, above 50DMA, above 200DMA) + dual-axis chart placeholder.
  - `dataClass="eod_breadth"`.
- [ ] Create `frontend/src/components/explore/DerivativesBlock.tsx`:
  - Fetches `/api/v1/derivatives/summary` with `dataClass="intraday"`.
  - `sparse=true` — renders `<EmptyState>` on `insufficient_data: true` (de_fo_bhavcopy is empty).
  - Does NOT render error state on empty data.
- [ ] Create `frontend/src/components/explore/YieldCurveBlock.tsx`:
  - Fetches `/api/v1/macros/yield-curve` with `{ tenors: "2Y,10Y,30Y,real" }`.
  - `dataClass="daily_regime"`.
  - Renders yield curve data as a line chart (Recharts `LineChart` with 4 lines).
  - Axis labels, legend, data source attribution per frontend-viz.md convention.
- [ ] Create `frontend/src/components/explore/InrChartBlock.tsx`:
  - Fetches `/api/v1/query` with `{ entity_type: "timeseries", filters: [...USDINR=X...] }`.
  - `sparse=true` — renders `<EmptyState>` on `insufficient_data: true` (de_global_price_daily has only 3 USDINR rows).
  - Does NOT render error on sparse data.
- [ ] Create `frontend/src/components/explore/FlowsBlock.tsx`:
  - Fetches `/api/v1/global/flows` with `{ scope: "india", range: "5y" }`.
  - `dataClass="daily_regime"`.
  - Renders FII/DII flow bar chart (Recharts `BarChart`).
  - `insufficient_data: true` → `<EmptyState>`.
- [ ] Create `frontend/src/components/explore/SectorsRRGBlock.tsx`:
  - Fetches `/api/v1/sectors/rrg` with `{ include: "gold_rs,conviction" }` and fixture `fixtures/sector_rrg.json`.
  - `dataClass="daily_regime"`.
  - Renders RRG scatter plot using Recharts `ScatterChart` — 4 quadrants (Leading, Improving, Weakening, Lagging).
  - Each sector point: symbol label, colour by quadrant per `quadrantColor()` from format.ts.
- [ ] Create `frontend/src/components/explore/ZoneEventsTable.tsx`:
  - Fetches `/api/v1/stocks/breadth/zone-events` with `{ universe: "nifty500", range: "5y" }`.
  - Fixture: `fixtures/zone_events.json`.
  - Renders signal history table with date, zone, duration columns.
- [ ] Create `frontend/src/components/explore/DivergencesCountryBlock.tsx`:
  - Fetches `/api/v1/stocks/breadth/divergences` with `{ universe: "nifty500" }`.
  - Renders divergence list; empty-state on empty divergences.
- [ ] All components use `DataBlock` for state management.
- [ ] All financial values formatted via format.ts utilities. No raw floats.
- [ ] Write `tests/unit/explore/test_explore_country_page.test.tsx` — at least 8 tests:
  - Page renders without crashing.
  - BreadthCompactBlock renders 3 KPI cards on mock data.
  - DerivativesBlock renders EmptyState on `insufficient_data: true` (not error).
  - InrChartBlock renders EmptyState on `insufficient_data: true`.
  - FlowsBlock renders BarChart on valid data.
  - SectorsRRGBlock renders scatter points with quadrant colours.
  - YieldCurveBlock renders 4 series lines on mock data.
  - ZoneEventsTable has correct column headers.
- [ ] Run `cd /home/ubuntu/atlas/frontend && npm test -- --passWithNoTests` — must exit 0.
- [ ] Run `cd /home/ubuntu/atlas/frontend && npm run build` — must exit 0.

## Exit criteria (measurable)

- `frontend/src/app/explore/country/page.tsx` exists and compiles.
- `frontend/src/components/explore/` contains at least 8 component files.
- All 8+ unit tests pass.
- `npm run build` exits 0.
- `DerivativesBlock` and `InrChartBlock` render `<EmptyState>` (not `<ErrorBanner>`) when `insufficient_data: true`.
- All charts have axis labels, legend, and tooltip per frontend-viz.md.

## Domain constraints

- Read `frontend/mockups/explore-country.html` before writing any component.
- Read `node_modules/next/dist/docs/` for Next.js 16 API before writing any page.
- `data-sparse="true"` blocks must use `sparse` prop on `DataBlock` or handle `insufficient_data`
  explicitly — they must NEVER show `<ErrorBanner>` on empty-data responses.
- `data-v2-derived="true"` blocks (signal-playback compact) must render `<EmptyState title="Coming soon" />`.
- Chart library: Recharts only (no D3, no Three.js for this page — data density does not require it).
- RRG scatter chart must have hover tooltip showing sector name, RS, gold RS, conviction values.
- Flows chart: right-align numbers, ₹ prefix, lakh/crore units.
