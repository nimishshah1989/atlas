# S2-3 — View 2: Leaders Board (`/leaders`)

**Slice:** `docs/specs/slices/s2-drill-spine.md` (read §4.3, §3.4, §5)
**Status:** PENDING  •  **Model:** sonnet  •  **Estimate:** $5 / 5h
**Depends on:** S2-1

## Mission

Build the Leaders Board at `/leaders` — the PM's "selection" view. Single screen: 3 toggles + RRG + leaders table. Regime gates the default universe (BULL→Stocks / CAUTIOUS→Sectors / CORRECTION→ETFs / BEAR→ETFs, §8 Q6).

## Scope

1. **Route:** `frontend/src/app/leaders/page.tsx`.

2. **Toggle rail:**
   - Universe: `Sectors | Stocks | ETFs | MFs` (initial value gated by `/api/v1/regime.posture`; PM override sticky in localStorage).
   - Benchmark: `N500 | Gold | MSCIW | SPX`.
   - Period: `1M | 3M | 6M | 12M`.
   - "Aligned leaders only" pill (default ON): RS-pct > 50 AND Momentum > 0 AND Abs-Return > 0 (§8 Q5).

3. **`<FourLensRRG>`** (from S2-1) wired to `GET /api/v1/leaders?universe=&benchmark=&period=&aligned_only=`. Click sector bubble → `/sector/[key]`. Click stock/ETF/MF bubble → instrument detail.

4. **`<InstrumentTable mode={universe}>`** below the RRG. Same endpoint, full 11-column schema. Row click → instrument page. "Size it" button → emits event to parent → opens `<ConvictionDrawer>` skeleton from S2-1 (real content wired in S2-6).

5. **Footer:** threshold transparency — print the 5 signal thresholds read-only from `/api/v1/signal/thresholds` (or whatever S2-0 exposes).

## Punch list

- [ ] `frontend/src/app/leaders/page.tsx` compiles + builds
- [ ] Initial universe matches regime posture (E2E: mock posture=CAUTIOUS → universe=Sectors)
- [ ] PM override of universe persists across reload via localStorage
- [ ] RRG bubble click routes correctly for all 4 universe types
- [ ] "Aligned leaders only" toggle actually narrows rows (unit test with fixture)
- [ ] "Size it" opens drawer skeleton without navigation
- [ ] ≥8 unit tests across toggles, row click, drawer trigger, regime-gate default
- [ ] `npm run build` exits 0; `python .quality/checks.py` frontend ≥80

## Hard stops

- Any local filtering that re-implements server-side `aligned_only` → STOP, pass to endpoint
- Hardcoding benchmark list → STOP, read from S2-0 response
- Opening the drawer in a new route → STOP, drawer is non-routing per §4.7

## Quality targets

```yaml
code: 80
frontend: 80
```

## Reference

Slice §4.3 (view spec), §3.4 (RRG mapping), §5 (column schema), §8 Q5/Q6 (aligned rule + regime-default gate).
