# S2-8 — Cross-view wiring + nav cleanup + v1 shell retirement

**Slice:** `docs/specs/slices/s2-drill-spine.md` (read §4.1, §10)
**Status:** PENDING  •  **Model:** sonnet  •  **Estimate:** $3 / 3h
**Depends on:** S2-0, S2-1, S2-2, S2-3, S2-4, S2-5, S2-6, S2-7

## Mission

Wire the 4 S2 views together, clean the top nav, and retire the v1 freelance shells (`/global`, `/india`, `/sector/*` old, `/etf/[ticker]` old shell). Close the §10 done-definition checklist.

## Scope

1. **Regime → Leaders gate:** confirm S2-3's default universe is actually gated by the live `/api/v1/regime.posture` value (not a static default). Add an E2E test that mocks posture=BEAR and asserts universe=ETFs on first render of `/leaders`.

2. **Sector links unified:** every link pointing at a sector (from Cockpit heatmap, Leaders RRG sector bubble, Leaders table row, Book rows carrying a sector) resolves to the same `/sector/[key]` route. Lint rule: grep the frontend for any remaining `/sector?key=` or `/sectors/` patterns and kill them.

3. **Nav cleanup:** update `frontend/src/components/nav/TopNav.tsx` (built in S1-7). New nav order: Cockpit (`/`) · Leaders · Book · [Explore dropdown: Breadth, MF Rank, Stock, Fund] · Methodology(drawer-hosted, no route). Remove links to any v1 shell.

4. **v1 shell retirement:** delete or redirect:
   - `/global` → delete route dir OR redirect to `/`
   - `/india` → delete OR redirect to `/`
   - old `/sector/*` freelance shell (if separate from S2-4's new page) → delete
   - old `/etf/[ticker]` → keep route, but ensure it renders the S2-5 6-tab structure
   - `/pulse` (from S1-1) → redirect to `/` (Cockpit supersedes it)

5. **Done-definition check:** run each §10 checkbox locally and paste results into the chunk report:
   - `python scripts/check-api-standard.py`
   - `python scripts/check-spec-coverage.py`
   - `python .quality/checks.py` — all 7 dims ≥80
   - Confirm no new Alembic migrations (`git diff backend/alembic/versions/ | wc -l` → 0 net-new files since S2-0)

## Punch list

- [ ] E2E: posture=BEAR → /leaders initial universe = ETFs
- [ ] grep `"/sector?key="\|"/sectors/"` returns 0 matches under `frontend/src/`
- [ ] `TopNav.tsx` lists exactly: Cockpit, Leaders, Book, Explore (dropdown), and zero links to retired shells
- [ ] `/global`, `/india`, `/pulse` either deleted or redirect (verify with curl -I → 200 for redirect target)
- [ ] `python scripts/check-api-standard.py` exits 0
- [ ] `python scripts/check-spec-coverage.py` exits 0
- [ ] `python .quality/checks.py` ≥80 on all 7 dims
- [ ] No net-new Alembic files since S2-0
- [ ] `npm run build` exits 0

## Hard stops

- Deleting a page that still has inbound links → STOP, add redirect first
- Touching any backend route (this is frontend-only wiring) → STOP, surface to new chunk if backend change needed
- Leaving two `/sector/*` route implementations live → STOP, retire the old one completely

## Quality targets

```yaml
code: 80
frontend: 80
api: 85
architecture: 80
```

## Reference

Slice §4.1 (route map), §10 (done-definition), memory `project_atlas_frontend_pages.md` (page tree).
